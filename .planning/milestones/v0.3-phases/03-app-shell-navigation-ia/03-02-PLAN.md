---
phase: 03-app-shell-navigation-ia
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - src/components/ui/collapsible.tsx
  - src/components/app-nav.tsx
  - src/components/mobile-nav.tsx
  - src/components/create/task-mode-page.tsx
  - src/app/(app)/upload/page.tsx
autonomous: true
requirements:
  - IA-02
  - IA-03
user_setup: []

must_haves:
  truths:
    - "主导航仅「项目」一层突出；创作台/对比/资料库在「更多工具」折叠组内且默认收起"
    - "Mobile Sheet 镜像 desktop 两级 IA（非扁平四项）"
    - "非活跃 nav 图标无 text-primary"
    - "/create 页有双书主路径提示条；/upload dual hero 动词与「新建双书项目」一致"
    - "npm test 全绿"
  artifacts:
    - path: "src/components/ui/collapsible.tsx"
      provides: "Radix collapsible primitive"
      contains: "Collapsible"
    - path: "src/components/app-nav.tsx"
      provides: "IA SSOT: PRIMARY + SECONDARY collapsible + FOOTER"
      contains: "更多工具"
    - path: "src/components/create/task-mode-page.tsx"
      provides: "/create compatibility strip"
      contains: "推荐主路径"
  key_links:
    - from: "src/components/app-nav.tsx"
      to: "src/components/ui/collapsible.tsx"
      via: "CollapsibleTrigger / CollapsibleContent"
      pattern: "from \"@/components/ui/collapsible\""
    - from: "src/components/mobile-nav.tsx"
      to: "src/components/app-nav.tsx"
      via: "AppNav only — no duplicate NAV_ITEMS"
      pattern: "<AppNav"
---

## Phase Goal

**As a** 登录用户, **I want to** 从全局壳层一眼进入双书项目与工作台, **so that** 30 秒内知道下一步而无需读文档。

<objective>
完成 IA-03 次要入口分层：AppNav 内「更多工具」Collapsible、nav 视觉层级与 D-05 图标色、mobile parity；`/create` 兼容提示与 `/upload` 文案对齐 D-24–D-25。

Purpose: 闭合四级顶栏竞争注意力问题；高级用户仍可直达 `/studio`、`/compare`、`/library`。
Output: shadcn Collapsible + 重构 `app-nav.tsx` + create/upload 文案。
</objective>

<execution_context>
@D:/workflow-agent/.cursor/get-shit-done/workflows/execute-plan.md
@D:/workflow-agent/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-app-shell-navigation-ia/03-CONTEXT.md
@.planning/phases/03-app-shell-navigation-ia/03-RESEARCH.md
@.planning/phases/03-app-shell-navigation-ia/03-PATTERNS.md
@.planning/phases/03-app-shell-navigation-ia/03-UI-SPEC.md
@.planning/phases/02-minimal-design-contract/02-UI-SPEC.md
@src/components/app-nav.tsx
@src/components/mobile-nav.tsx
@.planning/phases/03-app-shell-navigation-ia/03-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Collapsible primitive + AppNav IA refactor</name>
  <files>src/components/ui/collapsible.tsx, src/components/app-nav.tsx</files>
  <read_first>src/components/app-nav.tsx, src/components/ui/sheet.tsx, .planning/phases/03-app-shell-navigation-ia/03-RESEARCH.md, .planning/phases/03-app-shell-navigation-ia/03-UI-SPEC.md</read_first>
  <action>
    安装 shadcn Collapsible（RESEARCH Package Legitimacy：`@radix-ui/react-collapsible` [OK]）：
    `npx shadcn@latest add collapsible --yes`
    生成 `src/components/ui/collapsible.tsx`；勿手写 Radix 包装。

    重构 `app-nav.tsx`（D-01、D-02、D-03、D-04、D-05、D-14–D-16、D-22、D-23）：
    - 导出 `PRIMARY_NAV_ITEM`（`/sessions`「项目」）、`SECONDARY_NAV_ITEMS`（`/studio` 创作台、`/compare` 对比、`/library` 资料库）、`FOOTER_ITEMS`（`/settings` 设置）
    - `APP_NAV_ITEMS` = primary + secondary + footer（供测试/文档）
    - 渲染顺序：primary `NavLink` → `Collapsible` 默认 `useState(false)`（D-15，**无** localStorage）→ 组标题 **「更多工具」** `type-mono-label` + `ChevronRight` 旋转 → `CollapsibleContent` 内 secondary 列表 `pl-4`、`text-muted-foreground`
    - `NavLink` 增加 `variant: "primary" | "secondary"`；secondary 图标永远不用 `text-primary`
    - 活跃态：左侧 2px `bg-primary` 条保留；图标 active=`text-foreground`，inactive=`text-muted-foreground/70`（**删除** 现 105–107 行 `text-primary` on active icon，D-05）
    - 标签 `text-[13px]` → `type-body`
    - Primary active 范围（D-20 / RESEARCH A1）：**仅** `pathname === "/sessions"` 或 `pathname === "/"`；**不要** 对 `/sessions/[id]` 或 workbench 标 active
    - `FOOTER_ITEMS` 仍在 dashed border 分隔区（D-03）

    不修改 `/studio`、`/compare`、`/library` 页面本体（D-17）。
  </action>
  <verify>
    <automated>test -f src/components/ui/collapsible.tsx</automated>
    <automated>rg -q '更多工具' src/components/app-nav.tsx</automated>
    <automated>rg -q 'Collapsible' src/components/app-nav.tsx</automated>
    <automated>rg 'text-primary' src/components/app-nav.tsx | rg 'Icon' ; if ($LASTEXITCODE -eq 0) { exit 1 }</automated>
    <automated>rg -c 'href: "/sessions"' src/components/app-nav.tsx | rg -q '^1$'</automated>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    - `collapsible.tsx` 存在且由 shadcn CLI 生成
    - `app-nav.tsx` 含「更多工具」与 `SECONDARY_NAV_ITEMS`（studio/compare/library 三 href）
    - `app-nav.tsx` NavLink 图标 class 无 `active ? "text-primary"` 模式
    - `app-nav.tsx` 顶层 flat 四项 `NAV_ITEMS.map` 四级并列已移除
    - `FOOTER_ITEMS` 仍含 `/settings`
  </acceptance_criteria>
  <done>桌面主导航满足 D-01–D-05、D-14–D-16。</done>
</task>

<task type="auto">
  <name>Task 2: Mobile parity + shell nav regression gates</name>
  <files>src/components/mobile-nav.tsx, src/components/app-nav.tsx</files>
  <read_first>src/components/mobile-nav.tsx, src/components/app-nav.tsx, .planning/phases/03-app-shell-navigation-ia/03-PATTERNS.md</read_first>
  <action>
    确认 `mobile-nav.tsx`（D-04）：
    - **仅** import `AppNav`；不得复制 `NAV_ITEMS` / `SECONDARY_NAV_ITEMS` 数组
    - Sheet 内 `<AppNav onNavigate={() => setOpen(false)} />` 展示与 sidebar 相同两级结构（含折叠组）
    - Sheet 打开时「更多工具」默认收起（`AppNav` 内 `useState(false)` 随 remount 重置，D-15）
    - CTA 栈已在 03-01 对齐；本 task 仅验证未回退

    可选：在 `src/lib/shell/shell-title.test.ts` 追加一条文档性断言注释 — **不要** 为本 task 扩大范围。

    运行全量测试确保 nav 重构未破坏 dashboard 路由逻辑。
  </action>
  <verify>
    <automated>rg 'NAV_ITEMS|SECONDARY_NAV' src/components/mobile-nav.tsx ; if ($LASTEXITCODE -eq 0) { exit 1 }</automated>
    <automated>rg -q '<AppNav' src/components/mobile-nav.tsx</automated>
    <automated>rg -q '更多工具' src/components/app-nav.tsx</automated>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    - `mobile-nav.tsx` 无本地 nav 数组定义
    - `mobile-nav.tsx` 仍渲染 `AppNav`
    - `app-nav.tsx` 被 mobile 与 sidebar 共用（sidebar 仅 `<AppNav />`）
  </acceptance_criteria>
  <done>移动壳与桌面 IA 结构一致。</done>
</task>

<task type="auto">
  <name>Task 3: /create 提示条 + /upload 文案对齐</name>
  <files>src/components/create/task-mode-page.tsx, src/app/(app)/upload/page.tsx</files>
  <read_first>src/components/create/task-mode-page.tsx, src/app/(app)/upload/page.tsx, src/lib/upload/route.ts, .planning/phases/03-app-shell-navigation-ia/03-CONTEXT.md</read_first>
  <action>
    `task-mode-page.tsx`（D-24）：
    - 在 `PageHeader` 之后插入 `surface-subtle` 区块：`type-mono-label` **「推荐主路径」**；`type-body` 说明双书从「新建双书项目」进入；primary `Button asChild` → `/upload?mode=dual` 文案 **「新建双书项目」**；`type-caption` 注明当前页为任务模式（高级/兼容）
    - 现有推荐卡片 primary 按钮文案与 D-06 动词对齐（可保留「创建双书融合任务」若与 hero 一致，但 strip 上必须用「新建双书项目」）

    `upload/page.tsx`（D-25）：
    - **仅改** `PageHeader` dual 分支 `title`/`description`：动词含 **「双书」** 与 **「参考书」**；描述说明导入后进入工作台（与 03-01 redirect 一致，不再强调「先概览」为主路径）
    - **禁止** 修改 `resolveUploadRoute` 或 `redirect("/create")` 逻辑（RESEARCH Pitfall 5）

    壳层组件继续禁止新增 `text-[Npx]`（D-22）。
  </action>
  <verify>
    <automated>rg -q '推荐主路径' src/components/create/task-mode-page.tsx</automated>
    <automated>rg -q '新建双书项目' src/components/create/task-mode-page.tsx</automated>
    <automated>rg -q 'resolveUploadRoute' src/app/(app)/upload/page.tsx</automated>
    <automated>rg 'redirect\("/create"\)' src/app/(app)/upload/page.tsx</automated>
    <automated>npm test -- src/lib/upload/route.test.ts</automated>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    - `task-mode-page.tsx` 含「推荐主路径」与 `/upload?mode=dual` +「新建双书项目」
    - `upload/page.tsx` 仍调用 `resolveUploadRoute` 且 selector 仍 `redirect("/create")`
    - `upload/page.tsx` dual `PageHeader` 文案含「双书」
    - `upload/route.test.ts` 全绿
  </acceptance_criteria>
  <done>IA-02/03 文案链与 create 兼容入口完成；路由逻辑无回归。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → collapsible install | 新依赖进入 node_modules |
| browser → collapsed secondary links | 静态 href，无用户输入 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-06 | Tampering | shadcn/Radix install | mitigate | 仅 `npx shadcn@latest add collapsible`；RESEARCH 合法性表 `[OK]`；变更后 `npm test` |
| T-03-07 | Spoofing | secondary nav links | mitigate | href 固定 `/studio`、`/compare`、`/library` |
| T-03-08 | Repudiation | — | accept | 无审计日志需求 |
| T-03-09 | Information disclosure | — | accept | 无新数据面 |
| T-03-10 | DoS | — | accept | 客户端折叠状态仅内存 |
| T-03-SC | Tampering | @radix-ui/react-collapsible | mitigate | 安装前对照 `03-RESEARCH.md` Package Legitimacy Audit；拒绝 `[SLOP]` |

</threat_model>

<verification>
- `npm test` 全绿（含 `upload/route.test.ts`）
- `rg 'href: "/studio"' src/components/app-nav.tsx` 与 compare/library 均存在
- `rg '创作台' src/components/app-nav.tsx` 在 Collapsible 子级（非 primary 同级四项）
- 手动：打开 mobile Sheet →「更多工具」默认收起 → 展开可见三项次要链接
</verification>

<success_criteria>
- IA-03：1 突出 + 折叠次要 + footer 设置；mobile parity（D-01–D-05、D-14–D-17）
- IA-02：`/create` 与 `/upload` 文案闭环（D-24–D-25）
- 03-UI-SPEC § Alignment checklist 导航与 CTA 项可勾选
</success_criteria>

<output>
Create `.planning/phases/03-app-shell-navigation-ia/03-02-SUMMARY.md` when done
</output>
