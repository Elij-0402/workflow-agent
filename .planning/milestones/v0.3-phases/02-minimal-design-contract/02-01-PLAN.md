---
phase: 02-minimal-design-contract
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/02-minimal-design-contract/02-UI-SPEC.md
  - src/app/globals.css
  - src/lib/theme-tokens.ts
autonomous: true
requirements:
  - AUD-03
  - IA-01
user_setup: []

must_haves:
  truths:
    - "globals.css 含 8px spacing scale 变量且 .app-page 纵向节奏加大"
    - "theme-tokens.ts 展示 --info/--warning 且与 globals HSL 一致"
    - "02-UI-SPEC.md 骨架含 dark-only 书面决策、五级字号、panel 规则与 Top 15 映射"
    - "npm test 全绿"
  artifacts:
    - path: "src/app/globals.css"
      provides: "spacing + status tokens + typography utilities"
      contains: "--space-1"
    - path: "src/lib/theme-tokens.ts"
      provides: "TS 色板镜像"
      contains: "info"
    - path: ".planning/phases/02-minimal-design-contract/02-UI-SPEC.md"
      provides: "设计契约骨架"
      contains: "dark-only"
  key_links:
    - from: "02-UI-SPEC.md"
      to: "src/app/globals.css"
      via: "token 表引用 CSS 变量名"
      pattern: "--space-"
    - from: "globals.css"
      to: "theme-tokens.ts"
      via: "同步注释"
      pattern: "Keep in sync"
---

## Phase Goal

**As a** 实现者, **I want to** 把审计结论落成 token 与 SPEC 骨架, **so that** 02-02 能定稿契约并对齐 design-system。

<objective>
从 Top 15 backlog 提炼设计原则，在 `globals.css` / `theme-tokens.ts` 落地 spacing、status 色、字号 utility 草案，并创建 `02-UI-SPEC.md` 骨架（AUD-03 明/暗书面决策 + IA-01 token 表）。

Purpose: 单一事实来源的第一步 — CSS 先于组件迁移（D-22）。
Output: 可运行的 token 变更 + SPEC 骨架（非最终评审版）。
</objective>

<execution_context>
@D:/workflow-agent/.cursor/get-shit-done/workflows/execute-plan.md
@D:/workflow-agent/.cursor/get-shit-done/templates/summary.md
</execution_context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| Token 漂移 globals ↔ theme-tokens | medium | 同 PR 双文件 diff；design-system 仅读 TS 数组 |
| 误引入 light theme | low | 禁止第二套 `:root`；SPEC 写明 dark-only |
| 破坏 QLT-01 测试 | medium | 每 task 后 `npm test` |
</threat_model>

<context>
@.planning/phases/02-minimal-design-contract/02-CONTEXT.md
@.planning/phases/02-minimal-design-contract/02-RESEARCH.md
@.planning/phases/02-minimal-design-contract/02-PATTERNS.md
@.planning/phases/01-ux-audit-matrix/01-AUDIT-BACKLOG.md
@.planning/phases/01-ux-audit-matrix/01-UI-REVIEW.md
@.planning/REQUIREMENTS.md
@src/app/globals.css
@src/lib/theme-tokens.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Spacing scale 与 app-page 节奏</name>
  <files>src/app/globals.css</files>
  <read_first>src/app/globals.css, .planning/phases/02-minimal-design-contract/02-CONTEXT.md</read_first>
  <action>
    在 `:root` 添加 `--space-1` 至 `--space-6`（8px 倍数：8/16/24/32/40/48），注释标注 px 等价（D-05、D-09）。

    将 `.app-page` 的 `gap-7` 改为 `gap-10`（40px，对齐 D-05 下限）；保留 `max-w-[1280px]` 与 padding 不变。

    不修改任何 `(app)/**` 页面文件。
  </action>
  <verify>
    <automated>rg -q "--space-1" src/app/globals.css</automated>
    <automated>rg "gap-10" src/app/globals.css | rg -q "app-page"</automated>
  </verify>
  <acceptance_criteria>
    - `globals.css` `:root` 含 `--space-1` 与 `--space-6`
    - `.app-page` 使用 `gap-10` 而非 `gap-7`
    - 无 `(app)/` 下 tsx 文件在本 task 被修改
  </acceptance_criteria>
  <done>spacing 变量可在 design-system 下一 plan 引用。</done>
</task>

<task type="auto">
  <name>Task 2: 语义色 info/warning + data-label 修正</name>
  <files>src/app/globals.css, src/lib/theme-tokens.ts</files>
  <read_first>src/app/globals.css, src/lib/theme-tokens.ts, .planning/phases/02-minimal-design-contract/02-CONTEXT.md</read_first>
  <action>
    在 `:root` 添加 `--info` 与 `--warning` HSL 三元组（冷青 / 琥珀，具体值写入 UI-SPEC 草案表，D-20）；在 `@layer base` 或 utilities 暴露 `text-info`、`bg-info/10` 等最小集合（若项目已有 semantic 模式则对齐 shadcn 扩展）。

    SPEC 草案须写次级操作用 `outline`/`ghost`、破坏性用 `destructive`（D-19）。

    更新 `theme-tokens.ts`：`COLOR_TOKENS` 增加 `info`、`warning` 行，role 中文说明「进行中」「注意」。

    将 `.data-label` 的 `text-primary/80` 改为 `text-muted-foreground`（D-13）。

    不替换 workbench 内硬编码 amber/sky（Phase 4）。
  </action>
  <verify>
    <automated>rg -q '"info"' src/lib/theme-tokens.ts</automated>
    <automated>rg "text-muted-foreground" src/app/globals.css | rg -q "data-label"</automated>
    <automated>rg -q "--info" src/app/globals.css</automated>
  </verify>
  <acceptance_criteria>
    - `:root` 含 `--info` 与 `--warning`
    - `COLOR_TOKENS` 数组含 name `info` 与 `warning`
    - `.data-label` 不使用 `text-primary`
  </acceptance_criteria>
  <done>状态色有 token；装饰标签不再抢 CTA 色。</done>
</task>

<task type="auto">
  <name>Task 3: Surface 圆角与字号 utility 类</name>
  <files>src/app/globals.css</files>
  <read_first>src/app/globals.css, .planning/phases/02-minimal-design-contract/02-RESEARCH.md</read_first>
  <action>
    将 `.surface-panel`、`.surface-subtle`、`.surface-locked` 的 `rounded-[6px]` 改为 `rounded-[var(--radius-md)]`（5px，D-16）。

    在 `@layer components` 添加：`.type-display`、`.type-title`、`.type-body`、`.type-caption`、`.type-mono-label`（尺寸与字体族 per D-10；`type-display` 使用 `.chinese-display` + 约 text-xl；`type-mono-label` 复用 mono 栈 + muted）。

    保留现有 `.eyebrow-label` / `.mono-label-*`；在注释标明 deprecated 路径指向 `.type-mono-label`。

    内容区 panel 保持 `shadow-none`（D-15）— 不添加 box-shadow。
  </action>
  <verify>
    <automated>rg "radius-md" src/app/globals.css | rg -q "surface-panel"</automated>
    <automated>rg -q "type-display" src/app/globals.css</automated>
  </verify>
  <acceptance_criteria>
    - 三个 `surface-*` 使用 `--radius-md` 非 6px 字面量
    - 五个 `.type-*` 类存在于 globals.css
    - 无新增 `box-shadow` 于 surface-panel
  </acceptance_criteria>
  <done>圆角与字号阶梯在 CSS 可引用。</done>
</task>

<task type="auto">
  <name>Task 4: 撰写 02-UI-SPEC.md 骨架</name>
  <files>.planning/phases/02-minimal-design-contract/02-UI-SPEC.md</files>
  <read_first>.planning/phases/02-minimal-design-contract/02-CONTEXT.md, .planning/phases/01-ux-audit-matrix/01-AUDIT-BACKLOG.md, src/app/globals.css</read_first>
  <action>
    创建 `02-UI-SPEC.md`，结构至少包含：

    1. **§ 设计原则** — 极度克制、大留白；映射 backlog #1–2（panel 密度）、#9–11（字号/CTA）
    2. **§ 明/暗策略 (AUD-03)** — dark-only 决策 + D-02 三条理由 + 「未来浅色」附录占位（D-03）；写明保持 `html { color-scheme: dark; }` 且禁止 `light` class（D-04）
    3. **§ Spacing** — `--space-*` 表与 `app-page` gap 规则（D-05–D-07）
    4. **§ Typography** — 五级语义与禁止 `text-[Npx]`（D-10–D-12）
    5. **§ Surfaces & CTA** — 1+1 panel 规则、60/30/10、shadow 策略（D-08、D-14–D-21）
    6. **§ Migration inventory** — P0 文件列表（workbench、sessions page）标注 Phase 4 执行

    文内 MUST/禁止项用表格；引用实际 CSS 变量名。

    状态标为 **Draft — pending 02-02 review**。
  </action>
  <verify>
    <automated>rg -q "dark-only" .planning/phases/02-minimal-design-contract/02-UI-SPEC.md</automated>
    <automated>rg -q "Migration" .planning/phases/02-minimal-design-contract/02-UI-SPEC.md</automated>
  </verify>
  <acceptance_criteria>
    - `02-UI-SPEC.md` 存在且含明/暗、Spacing、Typography、Surfaces 四章
    - 文档含 `dark-only` 与 backlog Top 3 映射句
    - 含 Phase 4 迁移清单小节
  </acceptance_criteria>
  <done>审阅者可读 SPEC 骨架并对照 globals 变更。</done>
</task>

<task type="auto">
  <name>Task 5: 回归测试</name>
  <files></files>
  <read_first>package.json</read_first>
  <action>
    运行 `npm test`，失败则仅修复与本 plan token 变更相关的破坏（不扩 scope 到 workbench）。
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` 退出码 0
  </acceptance_criteria>
  <done>QLT-01 基线保持。</done>
</task>

</tasks>

<verification>
- `npm test` 通过
- `rg "--space-1" src/app/globals.css` 有匹配
- `02-UI-SPEC.md` 含 dark-only 决策
</verification>

<success_criteria>
- Token 草案与 CONTEXT D-05–D-21 一致
- SPEC 骨架可供 02-02 定稿
- 未修改 app-nav / workbench / sidebar 行为
</success_criteria>
