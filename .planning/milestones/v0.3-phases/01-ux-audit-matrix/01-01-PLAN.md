---
phase: 01-ux-audit-matrix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/01-ux-audit-matrix/01-FEATURE-MATRIX.md
autonomous: true
requirements:
  - AUD-01
user_setup: []

must_haves:
  truths:
    - "审阅者能在一份中文功能矩阵中看到全部 (app) 路由与 API 能力簇，并标注与双书主路径的关系"
    - "矩阵每行含 D-08 客观信号（导航层级与登录后步数区间）"
    - "双书主路径端到端旅程与单书/Studio 简线图可读且标注竞争注意力点"
  artifacts:
    - path: ".planning/phases/01-ux-audit-matrix/01-FEATURE-MATRIX.md"
      provides: "全站功能矩阵 + Mermaid 用户旅程"
      contains: "路由/入口"
    - path: ".planning/phases/01-ux-audit-matrix/01-FEATURE-MATRIX.md"
      provides: "API 能力聚类行"
      contains: "generate-v2"
  key_links:
    - from: "01-FEATURE-MATRIX.md"
      to: "src/components/app-nav.tsx"
      via: "客观信号列引用 APP_NAV_ITEMS"
      pattern: "APP_NAV_ITEMS|/sessions|/studio"
    - from: "01-FEATURE-MATRIX.md"
      to: "src/app"
      via: "路由枚举与 page.tsx 路径对照"
      pattern: "src/app/\\(app\\)"
---

## Phase Goal

**As a** 产品所有者, **I want to** 用证据看清全站入口与双书主路径的关系, **so that** Phase 2 能按优先级做设计决策而非凭感觉删功能。

<objective>
生成功能矩阵与主路径用户旅程图（AUD-01），落实 D-01–D-08、D-20–D-22。

Purpose: 为六柱 UI 审计与 Phase 2 UI-SPEC 提供路由×目标×频率×主路径关系的单一事实来源。
Output: `01-FEATURE-MATRIX.md`（含 Mermaid 主图 + 2 条附图，不单独拆 `01-USER-JOURNEYS.md` per D-22）。
</objective>

<execution_context>
@D:/workflow-agent/.cursor/get-shit-done/workflows/execute-plan.md
@D:/workflow-agent/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-ux-audit-matrix/01-CONTEXT.md
@.planning/phases/01-ux-audit-matrix/01-RESEARCH.md
@.planning/phases/01-ux-audit-matrix/01-PATTERNS.md
@.planning/codebase/STRUCTURE.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONCERNS.md
@src/components/app-nav.tsx
@src/components/sidebar.tsx
@src/app/(app)/dashboard/page.tsx
@src/app/(app)/sessions/archived/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: 路由与 API 清单写入矩阵骨架</name>
  <files>.planning/phases/01-ux-audit-matrix/01-FEATURE-MATRIX.md</files>
  <read_first>.planning/phases/01-ux-audit-matrix/01-CONTEXT.md, .planning/phases/01-ux-audit-matrix/01-RESEARCH.md, .planning/phases/01-ux-audit-matrix/01-PATTERNS.md, .planning/codebase/STRUCTURE.md</read_first>
  <action>
    创建 `01-FEATURE-MATRIX.md`（per D-04）。文档头使用 PATTERNS 模板：`# Phase 1: 全站功能矩阵`、**Audited:** 当日日期、**Scope:** `(app)` routes + API clusters + Server Actions（D-01）。

    运行枚举（Windows PowerShell 或 bash 等价）并写入 `## 路由清单（SSOT）` 小节表格，列：`File path` | `URL pattern` | `Audit tier` | `Notes`：
    - 枚举全部 `src/app/**/page.tsx`（RESEARCH 已列 16 条；执行时重新 glob 校验无遗漏）
    - URL 映射规则：去掉 `(app)`、`(auth)` 路由组；`[id]`、`[briefId]` 保留为矩阵记号
    - Audit tier：P0=`/sessions`、`/sessions/[id]`、`/sessions/[id]/workbench`（D-09）；P1=`/studio`、`/compare`、`/library`、`/settings`（D-10）；P2=`/create`、`/upload`、`/studio/new`、`/studio/[briefId]` 等（D-11）；Appendix=`/design-system`（D-12）；Edge 含 `/dashboard`、`/sessions/archived` 重定向（D-07）
    - `/` → 登录分流；`/login` 记为 auth 入口

    在同一文件添加 `## API 能力簇（非逐文件一行）`（D-01），8 簇各行：`Capability cluster` | `Routes` | `Matrix row hint`：Session lifecycle、Analyze（legacy+extended+chapter+book）、Blueprint（含 confirm/unconfirm）、Generate（`generate-v2` 标 hero，`generate` 标 legacy 边缘）、Studio briefs、Compare insights、Utilities（chapters/parse、variants）

    枚举 `src/app/api/**/route.ts` 与 `"use server"`：`src/lib/upload/actions.ts`、`src/app/(app)/settings/actions.ts`、`src/app/(auth)/actions.ts` — 供后续矩阵「关键依赖」列引用。

    禁止修改 `src/**`；禁止建议删除功能（PROJECT 先审计后减法）。
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "(Get-ChildItem -Path 'src/app' -Recurse -Filter page.tsx).Count -ge 15"</automated>
    <automated>rg -l "路由清单（SSOT）" ".planning/phases/01-ux-audit-matrix/01-FEATURE-MATRIX.md" | wc -l | rg -q "^1$"</automated>
    <automated>rg -c "generate-v2" ".planning/phases/01-ux-audit-matrix/01-FEATURE-MATRIX.md" | rg -q "[1-9]"</automated>
  </verify>
  <acceptance_criteria>
    - `01-FEATURE-MATRIX.md` 存在且含 `## 路由清单（SSOT）` 与 `## API 能力簇`
    - 路由表行数 ≥ 15（含 `/` 与 `/login`）
    - API 能力簇表含 Blueprint 与 Generate 两行，且 Generate 行提及 `generate-v2`
    - 全文无 `src/` 下文件被修改（仅 `.planning/phases/01-ux-audit-matrix/` 变更）
  </acceptance_criteria>
  <done>审阅者可对照 `src/app` 树核对矩阵 SSOT 表与 RESEARCH 清单一致或已标注 delta。</done>
</task>

<task type="auto">
  <name>Task 2: 填满 AUD-01 矩阵列与分类</name>
  <files>.planning/phases/01-ux-audit-matrix/01-FEATURE-MATRIX.md</files>
  <read_first>.planning/phases/01-ux-audit-matrix/01-FEATURE-MATRIX.md, .planning/phases/01-ux-audit-matrix/01-CONTEXT.md, src/components/app-nav.tsx, src/components/sidebar.tsx, src/app/(app)/dashboard/page.tsx, src/app/(app)/sessions/archived/page.tsx</read_first>
  <action>
    在 `01-FEATURE-MATRIX.md` 添加 `## 功能矩阵` 主表（per D-02），表头精确为：
    `| 路由/入口 | 用户目标 | 与双书主路径关系 | 频率 | 关键依赖 | 客观信号 | 备注 |`

    为 SSOT 中每个 `(app)` URL 至少一行（D-01 用户可感知入口粒度）：
    - **主**（D-05）：`/sessions` → `/sessions/[id]`（dual）→ `/sessions/[id]/workbench` → blueprint confirm → `POST /api/blueprint/confirm` → `POST /api/generate-v2`
    - **主路径支撑**（D-05）：`/create`、`/upload`（含 `?mode=dual`）合并为 **一行流程型入口**（D-03），关键依赖列写 `src/lib/upload/actions.ts` 及 analyze/generate 簇，不拆成重复 API 行
    - **次要**（D-06）：`/studio`、`/compare`、`/library`、`/settings`、单书 `/sessions/[id]` 非 dual 分支（备注 redirect 至 workbench 时 dual）
    - **边缘**（D-07）：`/design-system`、`/dashboard`（redirect `/sessions`）、`/sessions/archived`（redirect `/library`）、legacy `/api/generate`

    **客观信号**列（D-08）：从 `APP_NAV_ITEMS` 抄录四级顶栏（项目/创作台/对比/资料库）+ settings footer；写登录后步数区间（如 sidebar 顶栏 = 1 步；情境入口 = 3+ 步）

    **频率**列：高/中/低 — 用导航位置 + 产品形态推断（Claude discretion），无需遥测

    **备注**：标注 `generate` vs `generate-v2`、`analyze` vs `analyze/extended`、dual 409、CNV-* 仅记录不实施

    正文中文；路径/API 英文。
  </action>
  <verify>
    <automated>rg -c "^\| /sessions" ".planning/phases/01-ux-audit-matrix/01-FEATURE-MATRIX.md" | rg -q "[1-9]"</automated>
    <automated>rg "主路径支撑" ".planning/phases/01-ux-audit-matrix/01-FEATURE-MATRIX.md" | rg -c "upload|create" | rg -q "[1-9]"</automated>
    <automated>rg "客观信号" ".planning/phases/01-ux-audit-matrix/01-FEATURE-MATRIX.md" | wc -l | rg -q "^[1-9]"</automated>
  </verify>
  <acceptance_criteria>
    - `## 功能矩阵` 表头七列与 D-02 完全一致
    - 至少 3 行「与双书主路径关系」为 `主`，且含 `/sessions/[id]/workbench`
    - 恰好 1 行流程型入口合并 `/create` 与 `/upload`，且 `关键依赖` 含 `src/lib/upload/actions.ts`
    - `/dashboard` 与 `/sessions/archived` 备注含 redirect 目标
    - `客观信号` 列提及 sidebar 或 `APP_NAV_ITEMS` 四项同级导航
  </acceptance_criteria>
  <done>功能矩阵覆盖全部 (app) 路由与 API 主路径关系，满足 ROADMAP success criteria #1。</done>
</task>

<task type="auto">
  <name>Task 3: Mermaid 旅程图与回归门禁</name>
  <files>.planning/phases/01-ux-audit-matrix/01-FEATURE-MATRIX.md</files>
  <read_first>.planning/phases/01-ux-audit-matrix/01-FEATURE-MATRIX.md, .planning/phases/01-ux-audit-matrix/01-CONTEXT.md, .planning/codebase/ARCHITECTURE.md</read_first>
  <action>
    在 `01-FEATURE-MATRIX.md` 内添加 `## 用户旅程`（per D-22，不创建独立 `01-USER-JOURNEYS.md`）：

    **主图 1 张**（D-20）：Mermaid `flowchart LR`，节点含 `/upload?mode=dual` → `/sessions/[id]` → `/sessions/[id]/workbench` → 分析（chapter/book API）→ 对比/蓝图编辑 → `POST blueprint/confirm` → `POST generate-v2` → 结果阅读。在节点或边旁用 `痛点占位:` 标注槽位（如 `痛点占位: 待 01-UI-REVIEW`），禁止无证据臆测痛点。

    **附图 A**（D-21）：单书会话流，`flowchart LR`，≤8 步，末注 `竞争注意力: 创作台/对比/资料库 同级导航`

    **附图 B**（D-21）：Studio 简报流（`/studio` → `/studio/new` → `/studio/[briefId]`），≤8 步，同样标注竞争注意力

    运行 `npm test` 确认 188+ 基线未破坏（本 phase 不改 src，仅回归）。

    禁止运行 `npm run test:e2e` 或 `tests/e2e/smoke.spec.ts`（全量 LLM smoke 非本 plan 范围）。
  </action>
  <verify>
    <automated>rg -c "```mermaid" ".planning/phases/01-ux-audit-matrix/01-FEATURE-MATRIX.md" | rg -q "^[3-9]$|^[1-9][0-9]+$"</automated>
    <automated>rg "generate-v2" ".planning/phases/01-ux-audit-matrix/01-FEATURE-MATRIX.md" | rg -c "flowchart" | rg -q "[1-9]"</automated>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    - `## 用户旅程` 含至少 3 个 mermaid 代码块（主图 + 2 附图）
    - 主图含 `workbench` 与 `blueprint/confirm` 或 `generate-v2` 字样
    - 两条附图均含 `竞争注意力` 文案
    - `npm test` 退出码 0
  </acceptance_criteria>
  <done>矩阵文件自包含旅程图；AUD-01 可交付，01-02 可引用矩阵 tier 与路由列表。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| doc author → repo | 审计文档不得误改 `src/` |
| matrix → Phase 2 | 矩阵结论不得预决明/暗色（AUD-03 属 Phase 2） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | git commit scope | mitigate | `files_modified` 仅 phase 目录；执行后 `git diff --name-only` 无 `src/` |
| T-01-02 | Information Disclosure | 矩阵备注 | accept | 不粘贴 `.env` 或 API key |
| T-01-SC | Tampering | npm installs | accept | 本 plan 无新包安装 |
</threat_model>

<verification>
- `01-FEATURE-MATRIX.md` 含 SSOT、功能矩阵七列、用户旅程三节
- `rg -c "page.tsx" src/app` 行数与矩阵 `(app)` 行数一致或矩阵备注说明 redirect-only 页
- `npm test` 全绿
</verification>

<success_criteria>
- AUD-01 满足：路由×目标×频率×双书关系 + 客观信号 + API 簇
- D-01–D-08、D-20–D-22 均有可追溯实现
- 无 `src/` 变更
</success_criteria>

<output>
Create `.planning/phases/01-ux-audit-matrix/01-01-SUMMARY.md` when done
</output>
