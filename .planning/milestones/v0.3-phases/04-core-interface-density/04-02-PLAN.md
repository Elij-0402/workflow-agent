---
phase: 04-core-interface-density
plan: 02
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - src/app/(app)/sessions/page.tsx
  - src/app/(app)/sessions/SessionsClient.tsx
  - src/components/sessions/project-card.tsx
  - src/components/projects/project-overview-page.tsx
  - src/lib/projects/overview.ts
autonomous: true
requirements:
  - WB-02
user_setup: []

must_haves:
  truths:
    - "/sessions 首屏至多一个 hero 块（或单父级内 space-y-10 两段），指标默认折叠或 caption 行"
    - "SessionsClient 无双栏「本页规则」侧栏；单书区默认折叠"
    - "项目卡片：标题 + 一行 muted 下一步 + 语义状态徽章；nextHref 来自 deriveSessionDashboard"
    - "ProjectOverviewPage 首屏 surface-panel ≤2；主 CTA 动词与工作台一致"
  artifacts:
    - path: "src/app/(app)/sessions/page.tsx"
      provides: "合并 hero + Collapsible 指标"
    - path: "src/app/(app)/sessions/SessionsClient.tsx"
      provides: "全宽列表 + 单书 Collapsible + 情境 hint"
    - path: "src/components/projects/project-overview-page.tsx"
      provides: "概览 panel 预算 ≤2"
  key_links:
    - from: "src/components/sessions/project-card.tsx"
      to: "src/lib/sessions/dashboard.ts"
      via: "nextHref / nextActionLabel"
      pattern: "nextHref"
    - from: "src/app/(app)/sessions/page.tsx"
      to: "src/app/(app)/sessions/SessionsClient.tsx"
      via: "sessions prop"
      pattern: "SessionsClient"
---

## Phase Goal

**As a** 登录用户, **I want to** 在项目列表与概览页快速扫读状态与下一步, **so that** 首屏以项目列表为主、无仪表盘式噪声。

<objective>
重构 `/sessions` 与项目概览留白：合并 hero、折叠指标与单书区、移除规则侧栏、强化双书卡片扫读性、概览 panel 收敛。

Purpose: 闭合 backlog #6、#8、#49 与 WB-02（D-07–D-11）。
Output: 列表主导的首屏与 ≤2 panel 概览；不修改 `loadSessionDashboard` / dual redirect。
</objective>

<execution_context>
@D:/workflow-agent/.cursor/get-shit-done/workflows/execute-plan.md
@D:/workflow-agent/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-core-interface-density/04-CONTEXT.md
@.planning/phases/04-core-interface-density/04-PATTERNS.md
@.planning/phases/04-core-interface-density/04-UI-SPEC.md
@.planning/phases/03-app-shell-navigation-ia/03-UI-SPEC.md
@src/app/(app)/sessions/page.tsx
@src/app/(app)/sessions/SessionsClient.tsx
@src/components/sessions/project-card.tsx
@src/components/projects/project-overview-page.tsx
@src/lib/sessions/dashboard.ts
@src/lib/projects/overview.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sessions page hero merge + metrics collapse</name>
  <files>src/app/(app)/sessions/page.tsx</files>
  <read_first>src/components/app-nav.tsx, .planning/phases/04-core-interface-density/04-PATTERNS.md</read_first>
  <action>
    **合并主状态 + 快速入口**（D-07）：
    - 删除 `xl:grid-cols-[1.5fr_.9fr]` 双 `surface-panel` 并列。
    - 改为 **单一** `surface-panel p-6 space-y-6`：上块 eyebrow + `type-display` 活跃数 + `type-body` 说明；下块 `grid gap-3 sm:grid-cols-3` 三个 `LinkCard`（双书/单书/资料库）。
    - 外层与列表间：`space-y-10`（D-17）。

    **指标区**（D-08）：
    - 4× `MetricCard` 包进 `Collapsible`，`defaultOpen={false}`；触发器文案如 **项目统计** + `type-caption` 展开提示。
    - 或（planner 任选其一，须默认不占首屏）：折叠内改为一行 `type-caption`：`活跃 N · 待确认蓝图 M · …`。
    - `FolderKanban` 等图标：`text-primary` → `text-muted-foreground`（D-15 列表页部分）。

    **Typography**（D-16 本文件）：
    - `text-[28px]`/`text-[18px]`/`text-[13px]`/`text-[12px]` → `type-display`/`type-title`/`type-body`/`type-caption`。
    - **禁止新增** `text-[Npx]`。

    **保留** PageHeader primary → `/upload?mode=dual`「新建双书项目」（03-UI-SPEC，勿回归 `/create`）。
  </action>
  <verify>
    <automated>rg -q 'Collapsible' src/app/(app)/sessions/page.tsx</automated>
    <automated>rg -v '^#' src/app/(app)/sessions/page.tsx | rg 'xl:grid-cols-\[1\.5fr' ; if ($LASTEXITCODE -eq 0) { exit 1 }</automated>
    <automated>rg -q 'upload\?mode=dual' src/app/(app)/sessions/page.tsx</automated>
    <automated>rg -v '^#' src/app/(app)/sessions/page.tsx | rg 'text-\[[0-9]' ; if ($LASTEXITCODE -eq 0) { exit 1 }</automated>
  </verify>
  <done>首屏单一 hero；指标默认折叠；page.tsx 无任意 px 字号。</done>
</task>

<task type="auto">
  <name>Task 2: SessionsClient — full-width list + single-book Collapsible</name>
  <files>src/app/(app)/sessions/SessionsClient.tsx</files>
  <read_first>src/components/app-nav.tsx, src/components/workbench/hint-banner.tsx</read_first>
  <action>
    **布局**（D-09、D-10）：
    - 删除 `xl:grid-cols-[1.55fr_.85fr]` 与整个 `<aside>`（含「动态」「**本页规则**」两个 `surface-panel`）。
    - 主列 `w-full` + `space-y-10`；`lg+` 主列占满宽（无三栏侧栏竞争）。

    **情境 hint**（D-10）：
    - 列表顶 **一次** `surface-subtle` + `type-caption`（可复用 HintBanner 样式，文案 SSOT 固定一句，如 PATTERNS 示例：优先待确认蓝图与可生成项目；卡片「下一步」即推荐动作）。
    - **不要** 常驻规则 panel。

    **单书区**（D-09）：
    - `grouped.single` 外包 `Collapsible`，`defaultOpen={false}`；触发器：**单书兼容项目** + `type-caption` 计数。
    - 双书 `SessionSection` 保持主网格；bulk 选择条保留 `surface-panel`，字号 → `type-body`。

    **SessionSection 标题**：`text-[18px]` → `type-title`；description → `type-body text-muted-foreground`。

    **不要** 改 `deriveSessionDashboard`、筛选逻辑、批量 API。
  </action>
  <verify>
    <automated>rg '本页规则' src/app/(app)/sessions/SessionsClient.tsx ; if ($LASTEXITCODE -eq 0) { exit 1 }</automated>
    <automated>rg -q '单书兼容项目' src/app/(app)/sessions/SessionsClient.tsx</automated>
    <automated>rg -q 'Collapsible' src/app/(app)/sessions/SessionsClient.tsx</automated>
    <automated>rg 'xl:grid-cols-\[1\.55fr' src/app/(app)/sessions/SessionsClient.tsx ; if ($LASTEXITCODE -eq 0) { exit 1 }</automated>
  </verify>
  <done>无规则侧栏；单书默认折叠；双书列表全宽主导。</done>
</task>

<task type="auto">
  <name>Task 3: Project cards + overview panel budget</name>
  <files>src/components/sessions/project-card.tsx, src/components/projects/project-overview-page.tsx, src/lib/projects/overview.ts</files>
  <read_first>src/lib/sessions/dashboard.ts, src/lib/projects/overview.ts, .planning/phases/04-core-interface-density/04-UI-SPEC.md</read_first>
  <action>
    **project-card.tsx**（D-09）：
    - 主标题 `type-title`；下一步块压缩为一行 muted：`type-caption`「下一步」+ `type-body font-medium` 的 `nextActionLabel` + `type-caption` `lastActivityLabel`。
    - `href={session.nextHref || ...}` 保持；双书卡保留 `border-primary/25` 面强调（唯一允许的非 Button primary 面色）。
    - 装饰 `mono-label-sm text-primary` / `StagePill` 纯 primary 装饰 → `text-foreground` 或 `text-flash`/`text-info`/`text-locked` 语义映射（D-15 列表卡部分）。
    - 迁移 touched `text-[Npx]` → `type-*`。

    **project-overview-page.tsx**（D-11）：
    - 首屏 **≤2** 个 `surface-panel`：合并右侧重复说明为单一「下一步」panel 或把 `EditorialRecommendationPanel` 降为 `Collapsible`/`type-caption` 列表（不删除业务数据）。
    - 主 CTA：`Button asChild` + `Link` 至 `overview.nextAction.href`；标签来自 overview SSOT，须为 **进入工作台** / **确认蓝图**（与 04-03 文案表一致，可先改 `overview.ts`）。

    **overview.ts**（D-11、D-12 文案预备）：
    - `nextAction.label` / `statusLabel` 中「确认蓝图」「进入工作台」与 UI-SPEC § WB-03 表一致；删除冗长两句技术标题若存在。

    **不要** 改 `sessions/[id]/page.tsx` redirect 逻辑。
  </action>
  <verify>
    <automated>npm test -- src/lib/sessions/dashboard.test.ts</automated>
    <automated>rg -q 'nextHref' src/components/sessions/project-card.tsx</automated>
    <automated>rg -q '进入工作台' src/lib/projects/overview.ts</automated>
    <automated>rg -v '^#' src/components/sessions/project-card.tsx src/components/projects/project-overview-page.tsx | rg 'text-\[[0-9]' ; if ($LASTEXITCODE -eq 0) { exit 1 }</automated>
    <automated>npm test</automated>
  </verify>
  <done>卡片一眼可读；概览 ≤2 panel；overview 动词与工作台对齐；dashboard 测试全绿。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → list Link | href 来自 server 派生的 `nextHref` |
| RSC sessions page | 沿用 `safeGetUser` + `loadSessionDashboard` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04 | Spoofing | project-card href | mitigate | 不拼接用户输入到 path；仅用 `session.id` / `nextHref` |
| T-04-05 | Tampering | bulk selection API | accept | 不改批量删除请求体 |
| T-04-SC | Tampering | npm installs | mitigate | 无新包 |

</threat_model>

<verification>
- `npm test` 全绿（含 `dashboard.test.ts`）
- `/sessions` 首屏 DOM：一个 hero panel + 折叠指标 + SessionsClient
- 手动：单书区默认折叠；无双栏「本页规则」
</verification>

<success_criteria>
- WB-02：D-07–D-11 满足 `04-UI-SPEC.md` § WB-02
- Phase 3 shell CTA 与 dual workbench redirect 无回归
</success_criteria>

<output>
Create `.planning/phases/04-core-interface-density/04-02-SUMMARY.md` when done
</output>
