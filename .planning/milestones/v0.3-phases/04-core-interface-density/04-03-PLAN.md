---
phase: 04-core-interface-density
plan: 03
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - src/lib/ui/cta-copy.ts
  - src/lib/ui/cta-copy.test.ts
  - src/components/workbench/blueprint-editor.tsx
  - src/components/workbench/generate-drawer.tsx
  - src/components/workbench/hint-banner.tsx
  - src/components/workbench/chapter-tree.tsx
  - src/components/sessions/generate-panel.tsx
  - src/app/(app)/sessions/[id]/workbench/workbench-client.tsx
autonomous: true
requirements:
  - WB-03
user_setup: []

must_haves:
  truths:
    - "全站 confirm / generate / batch 文案来自 cta-copy SSOT"
    - "确认蓝图 Button 仅 blueprintReadyToConfirm 时可 default 且可点"
    - "蓝图未确认时生成阻断 toast 文案为「请先确认蓝图 — 前往对比蓝图步骤」"
    - "workbench + sessions 触及文件无装饰性 text-primary（除 Button default 主 CTA）"
    - "触及的 (app)/sessions/** 无新增 text-[Npx]"
  artifacts:
    - path: "src/lib/ui/cta-copy.ts"
      provides: "CTA_COPY 常量 SSOT"
      exports: ["CTA_COPY"]
    - path: "src/lib/ui/cta-copy.test.ts"
      provides: "文案契约单测"
    - path: "src/components/workbench/blueprint-editor.tsx"
      provides: "确认蓝图 disabled + caption"
      contains: "确认蓝图"
  key_links:
    - from: "src/components/workbench/blueprint-editor.tsx"
      to: "src/lib/blueprint/schema.ts"
      via: "blueprintReadyToConfirm"
      pattern: "blueprintReadyToConfirm"
    - from: "src/app/(app)/sessions/[id]/workbench/workbench-client.tsx"
      to: "src/lib/ui/cta-copy.ts"
      via: "import CTA_COPY"
      pattern: "CTA_COPY"
---

## Phase Goal

**As a** 双书项目用户, **I want to** 确认蓝图、生成与批量分析按钮在全站一致且状态清晰, **so that** 不会误点无效主按钮或读到装饰性高亮色。

<objective>
引入 CTA 文案 SSOT，统一 confirm / generate / batch 的 variant、disabled 与 toast；清除装饰性 primary；对 phase 触及路由做 typography 终检。

Purpose: WB-03（D-12–D-16）与 backlog #11、#53；依赖 04-01 布局后再改 workbench 按钮文案。
Output: `cta-copy` 单测 + grep 门禁 + `npm test` 全绿。
</objective>

<execution_context>
@D:/workflow-agent/.cursor/get-shit-done/workflows/execute-plan.md
@D:/workflow-agent/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-core-interface-density/04-CONTEXT.md
@.planning/phases/04-core-interface-density/04-UI-SPEC.md
@.planning/phases/02-minimal-design-contract/02-UI-SPEC.md
@src/lib/ui/cta-copy.ts
@src/lib/blueprint/schema.ts
@src/components/workbench/blueprint-editor.tsx
@src/components/workbench/generate-drawer.tsx
@src/app/(app)/sessions/[id]/workbench/workbench-client.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CTA_COPY SSOT + blueprint confirm wiring</name>
  <files>src/lib/ui/cta-copy.ts, src/lib/ui/cta-copy.test.ts, src/components/workbench/blueprint-editor.tsx</files>
  <behavior>
    - CTA_COPY.confirmBlueprint === "确认蓝图"
    - CTA_COPY.generateNewVersion === "生成新版本"
    - CTA_COPY.batchAnalysis === "批量分析"
    - CTA_COPY.enterWorkbench === "进入工作台"
    - CTA_COPY.blueprintBlockedToast 含「请先确认蓝图」
  </behavior>
  <action>
    新建 `src/lib/ui/cta-copy.ts`（纯常量 + 短 toast 串，无 React）导出 `CTA_COPY`（D-12、D-13、04-UI-SPEC § WB-03 表）。

    先写 `cta-copy.test.ts`（node:test）断言上列字符串 → RED → GREEN。

    **blueprint-editor.tsx**（D-12）：
    - 确认按钮文案 `CTA_COPY.confirmBlueprint`；`variant="default"` 且 `onClick` 仅当 `blueprintReadyToConfirm(bp).ok`。
    - 否则 `disabled` + 相邻 `type-caption` 列出 `ready.missing`（勿用 toast 代替 disabled 说明）。
    - 顶栏「生成新版本」用 `CTA_COPY.generateNewVersion`；仅 `status === "confirmed"` 显示。
    - draft badge `text-primary/85` → `text-muted-foreground`；confirmed 保持 `text-flash`。

    **不要** 改 confirm POST body 或 schema 校验逻辑。
  </action>
  <verify>
    <automated>npm test -- src/lib/ui/cta-copy.test.ts</automated>
    <automated>rg -q 'CTA_COPY' src/components/workbench/blueprint-editor.tsx</automated>
    <automated>rg -q '确认蓝图' src/components/workbench/blueprint-editor.tsx</automated>
  </verify>
  <done>CTA_COPY 单测通过；BlueprintEditor 确认按钮门禁与文案锁定。</done>
</task>

<task type="auto">
  <name>Task 2: Generate gate + batch primary + drawer parity</name>
  <files>src/components/workbench/generate-drawer.tsx, src/components/sessions/generate-panel.tsx, src/components/workbench/chapter-tree.tsx, src/app/(app)/sessions/[id]/workbench/workbench-client.tsx</files>
  <read_first>src/components/workbench/generate-drawer.tsx, src/app/(app)/sessions/[id]/workbench/workbench-client.tsx</read_first>
  <action>
    **workbench-client.tsx generate 步**（D-13）：
    - 主按钮文案统一 `CTA_COPY.generateNewVersion`（替换「再生成一版」「生成新小说」）。
    - `!compareDone` 提示：`type-caption text-muted-foreground` 或 `text-warning`，文案 `CTA_COPY.blueprintBlockedCaption`（或同等常量）；**禁止** `text-primary` 装饰（line ~856）。
    - 打开 drawer 前若蓝图未 confirmed：保留现有 gate，toast 用 `CTA_COPY.blueprintBlockedToast`（含前往 compare 步语义）。

    **generate-drawer.tsx** / **generate-panel.tsx**：
    - `submitLabel` 与触发按钮统一 `CTA_COPY.generateNewVersion`。
    - 配额确认 dialog 正文可保留 BYOK 说明，按钮 label 仍用 SSOT。

    **chapter-tree.tsx**（D-14）：
    - 批量主按钮 label → `CTA_COPY.batchAnalysis`；进行中态仍由 `BatchTracker` 展示（04-01 已保留 tracker）。
    - 批量失败 toast 若存在：对齐「批量分析失败 — 请检查章节后重试。」（04-UI-SPEC）。

    **hint-banner.tsx**（D-15）：
    - `text-primary/90` → `type-caption text-muted-foreground`；仍用 `deriveHint` 文案。

    **不要** 在本 task 做全文件 typography sweep（Task 3）。
  </action>
  <verify>
    <automated>rg -q '生成新版本' src/components/workbench/generate-drawer.tsx</automated>
    <automated>rg -q 'CTA_COPY' src/app/(app)/sessions/[id]/workbench/workbench-client.tsx</automated>
    <automated>rg -v '^#' src/components/workbench/hint-banner.tsx | rg 'text-primary' ; if ($LASTEXITCODE -eq 0) { exit 1 }</automated>
    <automated>npm test</automated>
  </verify>
  <done>生成/批量 CTA 文案统一；未确认蓝图 toast 与 muted 提示一致；HintBanner 无 primary 装饰。</done>
</task>

<task type="auto">
  <name>Task 3: Decorative primary purge + typography gate</name>
  <files>src/app/(app)/sessions/[id]/workbench/workbench-client.tsx, src/components/workbench/blueprint-editor.tsx, src/components/workbench/chapter-tree.tsx, src/components/workbench/generate-drawer.tsx, src/components/workbench/hint-banner.tsx, src/components/sessions/generate-panel.tsx, src/app/(app)/sessions/page.tsx, src/app/(app)/sessions/SessionsClient.tsx, src/components/sessions/project-card.tsx, src/components/projects/project-overview-page.tsx</files>
  <action>
    **D-15 清零**（仅 phase 已触及的 `(app)/sessions/**` 与 workbench 组件）：
    - `rg 'text-primary' ` 于上述路径：允许匹配仅在 `<Button variant="default">` 主 CTA 行；其余静态文本/图标/span 改为 `text-muted-foreground` 或语义 `text-flash`/`text-info`/`text-warning`/`text-destructive`。
    - 含 workbench `EmptySlot` 图标、`CollapsedChaptersBar`、`AnalysisCapabilityPanel` 残留 primary。

    **D-16 终检**：
    - 对上述路径：`rg 'text-\[[0-9]'` 须无匹配（04-01/04-02 应已迁移；本 task 修遗漏）。
    - **禁止** 新增任意 `text-[Npx]`。

    **overview / 列表**（若 04-02 已改 overview，仅核对）：
    - 概览与列表主 CTA import `CTA_COPY` where label is duplicated literal.

    运行 `npm test` 全量（QLT-01）。
  </action>
  <verify>
    <automated>npm test</automated>
    <automated>rg -v '^#' src/app/(app)/sessions src/components/workbench src/components/sessions/project-card.tsx src/components/projects/project-overview-page.tsx 2>$null | rg 'text-\[[0-9]' ; if ($LASTEXITCODE -eq 0) { exit 1 }</automated>
  </verify>
  <done>触及路由无装饰性 primary、无任意 px 字号；全站测试绿。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → blueprint confirm API | 不变；仅 UI disabled 门禁 |
| toast / copy | 无用户 HTML 注入 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-06 | Tampering | confirm handler | accept | 服务端仍校验 ready；UI disabled 不替代 API |
| T-04-07 | Repudiation | — | accept | 无新审计面 |
| T-04-SC | Tampering | npm installs | mitigate | 无新包 |

</threat_model>

<verification>
- `npm test` 全绿
- `npm test -- src/lib/ui/cta-copy.test.ts`
- 手动：蓝图未 ready 时确认按钮 disabled + caption；未 confirmed 点生成见 toast
</verification>

<success_criteria>
- WB-03：D-12–D-16 与 `04-UI-SPEC.md` § WB-03 对齐
- 每步 viewport 仅一个 `Button variant="default"` 主 CTA（与 04-01 布局一致）
</success_criteria>

<output>
Create `.planning/phases/04-core-interface-density/04-03-SUMMARY.md` when done
</output>
