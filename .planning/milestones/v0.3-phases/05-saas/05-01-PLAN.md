---
phase: 05-saas
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/e2e/smoke.spec.ts
  - .planning/phases/05-saas/05-SMOKE.md
  - scripts/tier-a-grep.ps1
autonomous: true
requirements:
  - QLT-01
user_setup: []

must_haves:
  truths:
    - "npm run type-check 与 npm test 全绿（≥197 tests）"
    - "smoke.spec.ts 使用 Phase 4 StepIntro 标题与 CTA_COPY 文案，无「第 2 步 · 分析…」「生成新小说」「再生成一版」"
    - "05-SMOKE.md 记录 test:e2e 前置、超时与最近一次手动成功日期"
    - "Tier A 主路径 grep 有脚本或 PLAN 内可复现命令；overview text-[Npx] 记为 baseline 债务而非 fail"
  artifacts:
    - path: "tests/e2e/smoke.spec.ts"
      provides: "双书 confirm → generate-v2 主路径 e2e SSOT"
      contains: "拆解章节"
    - path: ".planning/phases/05-saas/05-SMOKE.md"
      provides: "手动 e2e 运行手册"
      contains: "npm run test:e2e"
    - path: "scripts/tier-a-grep.ps1"
      provides: "Tier A 视觉 grep 门禁（可选执行）"
  key_links:
    - from: "tests/e2e/smoke.spec.ts"
      to: "src/lib/ui/cta-copy.ts"
      via: "getByRole name 对齐 CTA_COPY"
      pattern: "生成新版本"
---

## Phase Goal

**As a** 里程碑所有者, **I want to** 自动化回归与主路径冒烟可重复执行, **so that** QLT-01 在发布前有工程安全网且不因 Phase 4 文案漂移而假绿。

<objective>
保持单元测试全绿；将 `smoke.spec.ts` selector 对齐 Phase 4 工作台；产出 `05-SMOKE.md` 与 Tier A grep 工具。不新增路由/API；不重写 e2e 流程（D-02）。

Purpose: QLT-01（D-01–D-05、D-22）；为 05-02 人工验收提供绿基线。
Output: 绿 test + 对齐 smoke + `05-SMOKE.md` + `05-01-SUMMARY.md`。
</objective>

<execution_context>
@D:/workflow-agent/.cursor/get-shit-done/workflows/execute-plan.md
@D:/workflow-agent/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-saas/05-CONTEXT.md
@.planning/phases/05-saas/05-RESEARCH.md
@.planning/codebase/TESTING.md
@tests/e2e/smoke.spec.ts
@src/lib/ui/cta-copy.ts
@src/app/(app)/sessions/[id]/workbench/workbench-client.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Hard gate baseline + record test count</name>
  <read_first>package.json, .planning/phases/05-saas/05-CONTEXT.md, .planning/REQUIREMENTS.md</read_first>
  <action>
    执行硬性门禁（D-01）：`npm run type-check && npm test`。

    若失败：仅修复 **产品回归**（非环境）；不得删测试凑绿。

    在 plan 执行笔记或后续 SUMMARY 中记录通过数（当前基线 **197**，高于 REQUIREMENTS「188+」）。

    可选（D-05）：仅当发现缺口时，为 `deriveSessionDashboard` 或 shell redirect 补 **最小** 单测；非覆盖率 KPI。
  </action>
  <acceptance_criteria>
    - `npm run type-check` 退出码 0
    - `npm test` 输出含 `# pass 197` 或更高且无 `# fail` 大于 0
    - 无 `src/` 行为变更除非为修失败测试
  </acceptance_criteria>
  <verify>
    <automated>npm run type-check</automated>
    <automated>npm test</automated>
  </verify>
  <done>D-01 绿；test 计数写入执行记录。</done>
</task>

<task type="auto">
  <name>Task 2: Align smoke.spec.ts to Phase 4 UI</name>
  <read_first>tests/e2e/smoke.spec.ts, src/app/(app)/sessions/[id]/workbench/workbench-client.tsx, src/lib/ui/cta-copy.ts, .planning/phases/05-saas/05-RESEARCH.md</read_first>
  <action>
    仅修补 `tests/e2e/smoke.spec.ts` selector/流程（D-02），禁止新建平行 e2e spec。

    **标题替换（RESEARCH Pattern 2）：**
    - `"第 2 步 · 分析两本参考小说"` → `"拆解章节"`（workbench L741）
    - `"第 3 步 · 对比并整理骨架"` → `"整理融合蓝图"`（L793）
    - `"第 4 步 · 生成新小说"` → `"生成变体"`（L860）

    **生成步流程（D-02、04-03）：**
    - 删除 `getByRole("button", { name: "生成新小说", exact: true })`
    - 删除对 `"再生成一版"` 的 `toBeVisible` 断言
    - 蓝图 confirm 后 `page.reload()`；必要时点击 `"前往生成"`
    - 断言 heading `"生成变体"`
    - 点击 `"生成新版本"` 打开 drawer；在 sheet/dialog 内再点击 `"生成新版本"` 提交（若两个同名按钮，用 `page.getByRole('dialog').getByRole('button', { name: '生成新版本' })` 或 `.last()`）
    - 保留 `"生成结果"` heading 与 `"阅读全文 →"` → `article.reading-prose`

    **保持不变：** `"创建并进入工作台"`、`"前往对比"`、`"第 1 步 · 导入两本参考小说"`（upload 步）。

    任务结束前复跑 D-01 门禁。不在 CI 加 Playwright job（D-03）。
  </action>
  <acceptance_criteria>
    - `rg "第 2 步 · 分析两本参考小说" tests/e2e/smoke.spec.ts` 无匹配
    - `rg "生成新小说" tests/e2e/smoke.spec.ts` 无匹配
    - `rg "再生成一版" tests/e2e/smoke.spec.ts` 无匹配
    - `rg "拆解章节" tests/e2e/smoke.spec.ts` 至少 1 匹配
    - `rg "生成变体" tests/e2e/smoke.spec.ts` 至少 1 匹配
    - `npm run type-check && npm test` 退出码 0
  </acceptance_criteria>
  <verify>
    <automated>rg "第 2 步 · 分析" tests/e2e/smoke.spec.ts; if ($LASTEXITCODE -eq 0) { exit 1 }</automated>
    <automated>npm run type-check && npm test</automated>
    <manual>npm run test:e2e -- tests/e2e/smoke.spec.ts（有 .env + LLM 时；失败分类见 D-04）</manual>
  </verify>
  <done>smoke selector 与 Phase 4 UI 一致；单元门禁仍绿。</done>
</task>

<task type="auto">
  <name>Task 3: 05-SMOKE.md + Tier A grep script</name>
  <read_first>.planning/codebase/TESTING.md, tests/e2e/helpers/auth.ts, .planning/phases/05-saas/05-RESEARCH.md</read_first>
  <action>
    新建 `.planning/phases/05-saas/05-SMOKE.md`（D-03）须含：
    - 命令：`npm run test:e2e` 或 `npx playwright test tests/e2e/smoke.spec.ts`
    - 前置：根目录 `.env`（`NEXT_PUBLIC_SUPABASE_URL`、`SUPABASE_SERVICE_ROLE_KEY`）、`configureLlm` / BYOK；参考 `tests/e2e/helpers/auth.ts`
    - 超时建议：15min；LLM/网络失败记 **环境**（D-04），selector 失败记 **产品**
    - 表格：`Last successful run | date | operator | notes`
    - 初始行可写 `pending` 直至所有者手动跑通

    新建 `scripts/tier-a-grep.ps1`（D-11、RESEARCH Pattern 4）：
    - 对 Tier A 路径跑 `rg "text-\[[0-9]+px\]"` 与 workbench/upload 上 decorative `text-primary`
    - 路径：`sessions/page.tsx`、`SessionsClient.tsx`、`workbench-client.tsx`、`upload/`、`sessions/[id]/page.tsx`
    - 脚本 **exit 0** 且打印 `BASELINE_DEBT: sessions/[id]/page.tsx has N text-[Npx] (pre-Phase-5 allowlist)` 当仅 overview 有 legacy px；**exit 1** 若 workbench-client 或 sessions list 出现新匹配
    - 在 `05-SMOKE.md` 引用该脚本命令

    任务结束：`npm run type-check && npm test`。
  </action>
  <acceptance_criteria>
    - 文件 `.planning/phases/05-saas/05-SMOKE.md` 存在且含字符串 `npm run test:e2e`
    - 文件 `scripts/tier-a-grep.ps1` 存在
    - `05-SMOKE.md` 含 `Last successful run` 表头
    - `npm run type-check && npm test` 退出码 0
  </acceptance_criteria>
  <verify>
    <automated>Test-Path .planning/phases/05-saas/05-SMOKE.md</automated>
    <automated>Test-Path scripts/tier-a-grep.ps1</automated>
    <automated>npm run type-check && npm test</automated>
  </verify>
  <done>D-03 文档就绪；Tier A grep 可重复执行。</done>
</task>

<task type="auto">
  <name>Task 4 (optional): upload-branches e2e spot-check</name>
  <read_first>tests/e2e/upload-branches.spec.ts, .planning/phases/05-saas/05-CONTEXT.md</read_first>
  <action>
    若时间允许（D-05 discretion）：本地跑一次 `tests/e2e/upload-branches.spec.ts`；仅修 selector 漂移，不重写。

    若跳过：在 `05-01-SUMMARY.md` 记 `upload-branches: skipped`。

    仍须 D-01 绿。
  </action>
  <acceptance_criteria>
    - SUMMARY 或 commit message 注明 upload-branches 为 pass / skipped / fail-env
    - `npm test` 仍全绿
  </acceptance_criteria>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>可选分支回归已记录。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local `.env` → Playwright | 服务角色密钥仅本地；不得写入 git |
| smoke → Supabase admin | e2e 用 service role 种子数据；非生产用户数据 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Information disclosure | `.env` in CI logs | mitigate | Playwright 不进 CI；05-SMOKE.md 警告勿提交密钥 |
| T-05-02 | Tampering | smoke API posts | accept | 不改 `/api/*` 契约；仅 UI selector |
| T-05-03 | Repudiation | manual smoke date | mitigate | 05-SMOKE.md 要求 operator + date 行 |
| T-05-SC | Tampering | npm installs | accept | 本 plan 无新依赖 |

</threat_model>

<verification>
- `npm run type-check && npm test` 全绿
- `rg "生成新小说|再生成一版|第 2 步 · 分析" tests/e2e/smoke.spec.ts` 无匹配
- `05-SMOKE.md` 存在
- 手动 e2e（有环境时）记录于 05-SMOKE 表
</verification>

<success_criteria>
- QLT-01：单元全绿 + confirm→generate-v2 路径 selector 对齐 + 手动冒烟文档（D-01–D-05、D-22）
- 不修改产品功能代码（除非 test 暴露真实 bug）
</success_criteria>

<output>
Create `.planning/phases/05-saas/05-01-SUMMARY.md` when done
</output>
