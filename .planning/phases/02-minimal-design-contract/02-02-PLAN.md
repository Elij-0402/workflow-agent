---
phase: 02-minimal-design-contract
plan: 02
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - .planning/phases/02-minimal-design-contract/02-UI-SPEC.md
  - src/app/(app)/design-system/design-system-client.tsx
autonomous: true
requirements:
  - AUD-03
  - IA-01
user_setup: []

must_haves:
  truths:
    - "02-UI-SPEC.md 为 Approved 状态且章节覆盖 ROADMAP success criteria 三项"
    - "design-system 颜色/字体/间距/层级节与 SPEC 表格一致"
    - "实现者可仅凭 SPEC + design-system 验收 token，无需读 Phase 1 全文"
    - "npm test 全绿"
  artifacts:
    - path: ".planning/phases/02-minimal-design-contract/02-UI-SPEC.md"
      provides: "定稿设计契约"
      contains: "Approved"
    - path: "src/app/(app)/design-system/design-system-client.tsx"
      provides: "可视化对照"
      contains: "--space-"
  key_links:
    - from: "design-system-client.tsx"
      to: "02-UI-SPEC.md"
      via: "SECTION 文案与 token 表"
      pattern: "principles|spacing|color"
    - from: "02-UI-SPEC.md"
      to: "src/lib/theme-tokens.ts"
      via: "COLOR_TOKENS 引用"
      pattern: "info|warning"
---

## Phase Goal

**As a** 产品所有者, **I want to** 定稿 UI-SPEC 并对齐 design-system, **so that** Phase 3–4 有单一实现标准。

<objective>
定稿 `02-UI-SPEC.md`（间距、字号、色彩角色、边框/阴影/卡片），同步 `/design-system` 各节，产出对齐检查清单（AUD-03 + IA-01 完成）。本 plan 对应 ROADMAP 02-02 / CONTEXT D-23（定稿 SPEC + design-system 对齐）。

Purpose: 完成 ROADMAP Phase 2 全部 success criteria。
Output: Approved UI-SPEC + 更新的 design-system 参考页。
</objective>

<execution_context>
@D:/workflow-agent/.cursor/get-shit-done/workflows/execute-plan.md
@D:/workflow-agent/.cursor/get-shit-done/templates/summary.md
</execution_context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| SPEC 与 globals 不一致 | high | 02-02 开头 diff `rg` token 名；检查清单逐项 |
| design-system 误导生产 | low | 页脚注明 dev-only（D-24） |
</threat_model>

<context>
@.planning/phases/02-minimal-design-contract/02-UI-SPEC.md
@.planning/phases/02-minimal-design-contract/02-CONTEXT.md
@.planning/phases/02-minimal-design-contract/02-RESEARCH.md
@.planning/phases/02-minimal-design-contract/02-PATTERNS.md
@.planning/ROADMAP.md
@src/app/(app)/design-system/design-system-client.tsx
@src/lib/theme-tokens.ts
@src/app/globals.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: 定稿 02-UI-SPEC.md</name>
  <files>.planning/phases/02-minimal-design-contract/02-UI-SPEC.md</files>
  <read_first>.planning/phases/02-minimal-design-contract/02-UI-SPEC.md, .planning/phases/02-minimal-design-contract/02-CONTEXT.md, .planning/phases/02-minimal-design-contract/02-RESEARCH.md</read_first>
  <action>
    在 02-01 骨架基础上扩展为**完整** UI-SPEC：

    - 文首状态改为 **Approved — 2026-05-26**
    - **§ Color roles** — 完整 HSL 表（含 info/warning/primary/destructive/flash/locked/blocked）与 60/30/10 图示说明
    - **§ Typography** — 五级示例文案 + 禁止项 + `font-display` 使用边界
    - **§ Spacing** — `--space-*` 与 Tailwind class 对照表；`space-y-6` / `space-y-10` 使用场景（D-06）
    - **§ Surfaces** — surface-panel/subtle/locked 用途图；每视口 1 CTA 规则（D-08、D-18）；步骤页脚改为 sticky/footer 模式说明，不另包 surface-panel（D-17）
    - **§ Shadows & radius** — overlay-only；`--radius-md` 5px
    - **§ Appendix: Future light theme** — 仅变量命名约定（D-03）
    - **§ Alignment checklist** — 供本 plan Task 2 勾选

    每条 D-01–D-25 在文末 **Decision traceability** 表有一行 ID 引用。

    不修改 `(app)` 业务路由组件。
  </action>
  <verify>
    <automated>rg -q "Approved" .planning/phases/02-minimal-design-contract/02-UI-SPEC.md</automated>
    <automated>rg -c "D-0" .planning/phases/02-minimal-design-contract/02-UI-SPEC.md</automated>
  </verify>
  <acceptance_criteria>
    - SPEC 含 Color、Typography、Spacing、Surfaces、Shadows 五章实质内容（非占位）
    - 含 `Approved` 状态与明/暗书面理由（AUD-03）
    - Decision traceability 覆盖 D-01–D-25
  </acceptance_criteria>
  <done>SPEC 可作为 Phase 3 planner 唯一设计输入。</done>
</task>

<task type="auto">
  <name>Task 2: 同步 design-system 页面</name>
  <files>src/app/(app)/design-system/design-system-client.tsx</files>
  <read_first>src/app/(app)/design-system/design-system-client.tsx, .planning/phases/02-minimal-design-contract/02-UI-SPEC.md, src/lib/theme-tokens.ts</read_first>
  <action>
    更新 `design-system-client.tsx`：

    1. **#principles** — 文案与 SPEC 原则一致（克制、留白、单 CTA）
    2. **#color** — 渲染 `COLOR_TOKENS` 含 info/warning；添加 60/30/10 示意条
    3. **#type** — 展示 `.type-display` … `.type-mono-label` 样例（调用新 utility）
    4. **#spacing** — 展示 `--space-1`…`--space-6` 表与 `gap-10` 示例
    5. **#surface** — 更新 panel 说明：1 主 + 1 subtle 上限；圆角 5px
    6. **页脚** — 添加「实现标准页，非用户功能；生产 guard 属 P3」（D-24）

    禁止在 design-system 演示中新增 `text-[Npx]`（遵守 D-11 精神）。

    可使用 `design-system` 路由内既有 `surface-panel` 演示结构。
  </action>
  <verify>
    <automated>rg -q "info" src/app/(app)/design-system/design-system-client.tsx</automated>
    <automated>rg -q "type-display" src/app/(app)/design-system/design-system-client.tsx</automated>
    <automated>rg -q "space-1" src/app/(app)/design-system/design-system-client.tsx</automated>
  </verify>
  <acceptance_criteria>
    - design-system 的 color 节展示 info/warning
    - type 节展示至少 3 个 `.type-*` 样例
    - spacing 节含 `--space-` 引用
    - 页脚含非用户功能声明
  </acceptance_criteria>
  <done>浏览器打开 `/design-system` 可目视验收 token。</done>
</task>

<task type="auto">
  <name>Task 3: 对齐检查清单与 REQ 闭环</name>
  <files>.planning/phases/02-minimal-design-contract/02-UI-SPEC.md</files>
  <read_first>.planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/phases/02-minimal-design-contract/02-UI-SPEC.md</read_first>
  <action>
    在 `02-UI-SPEC.md` 末尾添加 `## Phase 2 验收清单`，逐项勾选（markdown `[x]`）：

    - [ ] UI-SPEC 定义间距、字号、色彩、边框/阴影/卡片（ROADMAP SC1）
    - [ ] 明/暗策略书面决策与审计一致（ROADMAP SC2 / AUD-03）
    - [ ] design-system 与 token 对齐（ROADMAP SC3 / IA-01）

    运行 token 一致性检查：`rg` 比对 globals `:root` 变量名与 SPEC 表、theme-tokens 数组 name 字段。

    更新 REQUIREMENTS traceability 备注（仅 planning 文档）：在 commit message 或 SUMMARY 中声明 AUD-03、IA-01 将由 verify-phase 标 Complete。

    运行 `npm test`。
  </action>
  <verify>
    <automated>rg -q "Phase 2 验收清单" .planning/phases/02-minimal-design-contract/02-UI-SPEC.md</automated>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    - SPEC 含验收清单且三项为 `[x]`
    - `npm test` 退出码 0
    - globals / theme-tokens / SPEC 中 `--info` 一致存在
  </acceptance_criteria>
  <done>Phase 2 设计契约可进入 /gsd-execute-phase。</done>
</task>

</tasks>

<verification>
- 打开 `/design-system` 目视：色板含 info/warning、间距表存在
- `02-UI-SPEC.md` 状态 Approved
- `npm test` 通过
</verification>

<success_criteria>
- ROADMAP Phase 2 三项 success criteria 均有 SPEC + design-system 证据
- AUD-03、IA-01 需求在定稿 SPEC 中可追溯
- 无 sidebar/workbench 代码变更
</success_criteria>
