---
phase: 01-ux-audit-matrix
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - .planning/phases/01-ux-audit-matrix/01-UI-REVIEW.md
  - .planning/phases/01-ux-audit-matrix/01-AUDIT-BACKLOG.md
  - .planning/ui-reviews/.gitignore
autonomous: false
requirements:
  - AUD-02
user_setup: []

must_haves:
  truths:
    - "01-UI-REVIEW.md 含 P0 六柱 1–4 分与 Overall /24（不含 design-system 与 P1 拉低均分）"
    - "每条 backlog 含 severity、route/file、pillar、现象、建议方向且可对应源码"
    - "Top 15 摘要与完整 backlog 足以支撑 Phase 2 设计决策"
  artifacts:
    - path: ".planning/phases/01-ux-audit-matrix/01-UI-REVIEW.md"
      provides: "六柱 UI 审计报告"
      contains: "Pillar Scores"
    - path: ".planning/phases/01-ux-audit-matrix/01-AUDIT-BACKLOG.md"
      provides: "可执行 backlog"
      contains: "Top 15"
  key_links:
    - from: "01-AUDIT-BACKLOG.md"
      to: "01-UI-REVIEW.md"
      via: "BLOCKER/WARNING 映射为 P0/P1"
      pattern: "severity.*route/file"
    - from: "01-UI-REVIEW.md"
      to: "src/app/(app)/sessions/[id]/workbench/workbench-client.tsx"
      via: "P0 文件列表与 file:line 证据"
      pattern: "workbench-client"
---

## Phase Goal

**As a** 产品所有者, **I want to** 六柱 UI 证据与分级 backlog, **so that** Phase 2 能按严重度收敛密度与层级而非局部修补。

<objective>
运行六柱 UI 审计并汇总可执行 backlog（AUD-02），落实 D-09–D-19、D-14 三层证据与 D-15 Top 15。

Purpose: 产出 UI-REVIEW 等价物与 Phase 2–5 优先级输入；P0 为评分基准（D-13）。
Output: `01-UI-REVIEW.md`、`01-AUDIT-BACKLOG.md`；可选 `.planning/ui-reviews/` 截图（gitignored）。
</objective>

<execution_context>
@D:/workflow-agent/.cursor/get-shit-done/workflows/execute-plan.md
@D:/workflow-agent/.cursor/get-shit-done/templates/summary.md
@D:/workflow-agent/.cursor/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-ux-audit-matrix/01-CONTEXT.md
@.planning/phases/01-ux-audit-matrix/01-RESEARCH.md
@.planning/phases/01-ux-audit-matrix/01-PATTERNS.md
@.planning/phases/01-ux-audit-matrix/01-FEATURE-MATRIX.md
@.cursor/agents/gsd-ui-auditor.md
@.cursor/get-shit-done/workflows/ui-review.md
@.cursor/get-shit-done/references/ui-brand.md
@.planning/codebase/CONCERNS.md
@src/app/(app)/sessions/page.tsx
@src/app/(app)/sessions/SessionsClient.tsx
@src/app/(app)/sessions/[id]/page.tsx
@src/app/(app)/sessions/[id]/workbench/page.tsx
@src/app/(app)/sessions/[id]/workbench/workbench-client.tsx
@src/app/(app)/studio/page.tsx
@src/app/(app)/compare/page.tsx
@src/app/(app)/library/page.tsx
@src/app/(app)/settings/page.tsx
@src/app/(app)/design-system/page.tsx
@tests/e2e/smoke.spec.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: P0 静态六柱 + 派发 gsd-ui-auditor</name>
  <files>.planning/phases/01-ux-audit-matrix/01-UI-REVIEW.md</files>
  <read_first>.cursor/agents/gsd-ui-auditor.md, .planning/phases/01-ux-audit-matrix/01-CONTEXT.md, .planning/phases/01-ux-audit-matrix/01-PATTERNS.md, .planning/phases/01-ux-audit-matrix/01-FEATURE-MATRIX.md, src/app/(app)/sessions/[id]/workbench/workbench-client.tsx</read_first>
  <action>
    **禁止**调用 stock `/gsd-ui-review` 工作流（会因缺少 `*-SUMMARY.md` 退出，见 RESEARCH Pitfall 1）。必须使用 **Task 工具** `subagent_type="gsd-ui-auditor"` 直接派发。

    派发 prompt 须包含：
    - 输出路径：`.planning/phases/01-ux-audit-matrix/01-UI-REVIEW.md`
    - Baseline：无 UI-SPEC；abstract 6-pillar（D-16）
    - **Scoring scope:** P0 files only for Overall /24（D-13）
    - P0 文件列表（D-09）：`sessions/page.tsx`、`SessionsClient.tsx`、`sessions/[id]/page.tsx`、`workbench/page.tsx`、`workbench-client.tsx`
    - 要求每条 finding 含 `file:line`；BLOCKER→backlog P0，WARNING→P1/P2 映射说明写入 review 文末
    - 审计 **密度/层级/面板数**，禁止「应改浅色模式」类 AUD-03 结论

    执行者在本 task 同步运行静态证据（D-14 层 2）并写入 review 或附于 auditor 输入：
    - `wc -l` workbench-client.tsx（≥500 行标密度；~1361 为已知信号）
    - `rg -n "surface-panel" workbench-client.tsx` 计数
    - 六柱 grep 套件（PATTERNS / gsd-ui-auditor）：Submit/Click Here、text-* 字号、arbitrary `[Npx]`、loading/skeleton
    - `rg -n "第 [0-9] 步" src/app/(app)/sessions --glob "*.tsx"`

    子代理返回后，合并为 `01-UI-REVIEW.md`，shell 结构 per PATTERNS：
    `# Phase 1 — UI Review`、**Scoring scope:** P0 only、`## Pillar Scores`（6 行 1–4）、**Overall: {n}/24**、Top 3 Priority Fixes、Detailed Findings（每柱至少 1 条证据）、Files Audited。

    不修改 `src/**`。
  </action>
  <verify>
    <automated>test -f ".planning/phases/01-ux-audit-matrix/01-UI-REVIEW.md"</automated>
    <automated>rg "Overall:" ".planning/phases/01-ux-audit-matrix/01-UI-REVIEW.md" | rg -c "/24" | rg -q "[1-9]"</automated>
    <automated>rg -c "Pillar Scores" ".planning/phases/01-ux-audit-matrix/01-UI-REVIEW.md" | rg -q "^1$"</automated>
    <automated>rg "workbench-client" ".planning/phases/01-ux-audit-matrix/01-UI-REVIEW.md" | wc -l | rg -q "^[1-9]"</automated>
  </verify>
  <acceptance_criteria>
    - `01-UI-REVIEW.md` 存在且含 `## Pillar Scores` 与 `Overall:` 且分母为 24
    - `Files Audited` 或正文列出全部 5 个 P0 源文件路径
    - Detailed Findings 至少 6 条含 `:` 行号或 `.tsx` 路径模式
    - 全文未将 `/design-system` 计入 Overall /24 计算说明（D-12）
    - 未通过 `/gsd-ui-review` 编排器单独跑通（仅 direct auditor）
  </acceptance_criteria>
  <done>P0 六柱评分与证据落盘，可供 backlog 合成。</done>
</task>

<task type="auto">
  <name>Task 2: P1/P2 走查、附录与 P0 浏览器冒烟</name>
  <files>.planning/phases/01-ux-audit-matrix/01-UI-REVIEW.md, .planning/ui-reviews/.gitignore</files>
  <read_first>.planning/phases/01-ux-audit-matrix/01-UI-REVIEW.md, .planning/phases/01-ux-audit-matrix/01-FEATURE-MATRIX.md, src/app/(app)/studio/page.tsx, src/app/(app)/compare/page.tsx, src/app/(app)/library/page.tsx, src/app/(app)/settings/page.tsx, src/app/(app)/design-system/page.tsx, .planning/codebase/CONCERNS.md, tests/e2e/smoke.spec.ts, .cursor/agents/gsd-ui-auditor.md</read_first>
  <action>
    扩展 `01-UI-REVIEW.md`（不降低 P0 Overall /24，D-13）：

    **P1 全量走查**（D-10）：`/studio`、`/compare`、`/library`、`/settings` 对应 page + 关键 components。可对重复密度/字号问题在 review 记「合并候选」供 Task 3 dedupe。

    **P2 spot-check**（D-11）：`/create`、`/upload`、`/studio/new`、`/studio/[briefId]`、`/sessions/archived` — 清单式明显违规（每页 ≤5 条 bullet），无六柱长文。

    添加 `## Appendix: /design-system`（D-12）：标注为 token 参考源、非产品 UX 达标页；记录 CONCERNS 中生产暴露风险 → 指向 backlog P3，**不计入** Pillar 均分。

    添加 `## P1/P2 Findings (backlog feed)` 小节：每条含 route、pillar、现象、建议方向（中文现象，英文路径）。

    **D-14 层 3 轻量 P0 冒烟**（同 task 内完成，非 `tests/e2e/smoke.spec.ts`）：
    1. `mkdir -p .planning/ui-reviews` 并写入 `.gitignore`（`*.png` 等）
    2. `npm run dev` → `http://localhost:3000`（不可用则 review 记 `Browser smoke: skipped`）
    3. 登录 → `/sessions` → 已有 dual session 的 workbench；无种子则 skeleton-only
    4. 可见 `第 2 步` heading 或步骤 chrome；禁止 analyze/generate API 调用
    5. 更新 **Screenshots:** captured | not captured | skeleton-only；MCP browser 或 Playwright 截图仅存 ui-reviews

    禁止建议删除 Studio/对比/单书功能。
  </action>
  <verify>
    <automated>rg "## Appendix: /design-system" ".planning/phases/01-ux-audit-matrix/01-UI-REVIEW.md" | wc -l | rg -q "^1$"</automated>
    <automated>rg "/studio" ".planning/phases/01-ux-audit-matrix/01-UI-REVIEW.md" | wc -l | rg -q "^[1-9]"</automated>
    <automated>rg "Screenshots:" ".planning/phases/01-ux-audit-matrix/01-UI-REVIEW.md" | wc -l | rg -q "^[1-9]"</automated>
    <automated>test -f ".planning/ui-reviews/.gitignore" || rg -q "Browser smoke: skipped" ".planning/phases/01-ux-audit-matrix/01-UI-REVIEW.md"</automated>
  </verify>
  <acceptance_criteria>
    - `01-UI-REVIEW.md` 含 `## Appendix: /design-system` 且说明 excluded from Overall
    - P1 四路由 `/studio`、`/compare`、`/library`、`/settings` 均出现在 review 正文
    - P2 小节提及 `/create` 或 `/upload` 至少一处
    - `Screenshots:` 行存在；`.planning/ui-reviews/.gitignore` 存在或 review 声明 browser skipped
    - Overall /24 数值未因 P1 条目重算（文内说明 P0-only baseline）
  </acceptance_criteria>
  <done>AUD-02 覆盖完整；P0 具备代码+可选视觉证据。</done>
</task>

<task type="auto">
  <name>Task 3: 合成 backlog 与 Top 15</name>
  <files>.planning/phases/01-ux-audit-matrix/01-AUDIT-BACKLOG.md</files>
  <read_first>.planning/phases/01-ux-audit-matrix/01-UI-REVIEW.md, .planning/phases/01-ux-audit-matrix/01-FEATURE-MATRIX.md, .planning/phases/01-ux-audit-matrix/01-CONTEXT.md, .planning/phases/01-ux-audit-matrix/01-PATTERNS.md</read_first>
  <action>
    创建 `01-AUDIT-BACKLOG.md`（D-19），中文正文。

    文首：`# Phase 1: 审计 Backlog`、**Synthesized:** 日期、**Dedupe key:** `lower(route)|pillar|normalize(现象)`

    **`## Top 15（摘要）`**（D-15）：表格列 `# | severity | route/file | 现象（一句话）`，恰好 15 行（不足则全列 P0/P1 后标注「&lt;15 条有效项」）

    **`## 完整 Backlog`** 主表（D-18）表头：
    `| severity | route/file | pillar或类别 | 现象 | 建议方向 |`
    - severity 仅用 `P0`|`P1`|`P2`|`P3`（非 BLOCKER 字面）
    - 从 UI-REVIEW 映射：BLOCKER→P0；WARNING→P1（P0 路由）或 P2（P1/P2 路由）
    - 合并重复 P1（D-21 discretion）：单行 `route/file` 列写 `` `/studio`, `/compare` ``，备注 `affected_routes`

    **`## Deferred (P3 — 本里程碑不实施)`**：`/design-system` 生产 guard、CNV-01 legacy `/api/generate` 等（来自 CONCERNS / CONTEXT deferred）

    交叉引用矩阵：主路径 P0 项须含 `/sessions/[id]/workbench` 至少 3 条。

    运行 `npm test` 回归。
  </action>
  <verify>
    <automated>rg "## Top 15" ".planning/phases/01-ux-audit-matrix/01-AUDIT-BACKLOG.md" | wc -l | rg -q "^1$"</automated>
    <automated>rg "^\| P0 \|" ".planning/phases/01-ux-audit-matrix/01-AUDIT-BACKLOG.md" | wc -l | rg -q "^[1-9]"</automated>
    <automated>rg "severity \| route/file \| pillar" ".planning/phases/01-ux-audit-matrix/01-AUDIT-BACKLOG.md" | wc -l | rg -q "^[1-9]"</automated>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    - 三件套齐全：`01-FEATURE-MATRIX.md`、`01-UI-REVIEW.md`、`01-AUDIT-BACKLOG.md` 均存在
    - Top 15 表存在且 severity 列仅含 P0/P1/P2/P3
    - 完整表每行五列齐全（允许备注列扩展但五列必填）
    - backlog 至少 1 条 `workbench` 或 `/sessions/[id]/workbench`
    - `npm test` 退出码 0
  </acceptance_criteria>
  <done>可执行 backlog 与 Top 15 就绪，满足 ROADMAP success criteria #2–#3 的文档侧条件。</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Phase 2 就绪度确认</name>
  <files>.planning/phases/01-ux-audit-matrix/01-FEATURE-MATRIX.md, .planning/phases/01-ux-audit-matrix/01-UI-REVIEW.md, .planning/phases/01-ux-audit-matrix/01-AUDIT-BACKLOG.md</files>
  <action>暂停执行，将三件套交付所有者抽查（D-15）。在收到 resume 信号前不得标记 Phase 1 完成或启动 Phase 2 规划。</action>
  <verify>
    <human-check>
      1. 打开 `01-FEATURE-MATRIX.md` — 主路径行与 Mermaid 主图可读。
      2. 打开 `01-UI-REVIEW.md` — Overall /24 仅 P0；design-system 附录未计入均分。
      3. 打开 `01-AUDIT-BACKLOG.md` — Top 15 足以支撑 Phase 2 优先级讨论。
      回复 `approved` 或列出缺口。
    </human-check>
  </verify>
  <acceptance_criteria>
    - Resume 信号为 `approved`（不区分大小写）或用户列出可操作的缺口列表
    - 三件套文件在 checkpoint 时均存在且非空
  </acceptance_criteria>
  <done>所有者确认审计结论足以支撑 Phase 2（ROADMAP success criteria #3）。</done>
  <what-built>Phase 1 三件套：`01-FEATURE-MATRIX.md`（矩阵+旅程）、`01-UI-REVIEW.md`（P0 /24 + P1/P2/附录）、`01-AUDIT-BACKLOG.md`（Top 15 + 完整表）。无 `src/` 变更。</what-built>
  <how-to-verify>
    1. 打开 `.planning/phases/01-ux-audit-matrix/01-FEATURE-MATRIX.md` — 确认主路径行与 Mermaid 主图可读。
    2. 打开 `01-UI-REVIEW.md` — 确认 Overall /24 仅反映 P0；附录 design-system 未计入均分。
    3. 打开 `01-AUDIT-BACKLOG.md` — 浏览 Top 15，判断是否足以支撑 Phase 2（明/暗、密度、IA）优先级讨论（D-15）。
    4. 若同意，回复 `approved`；若不足，列出缺失路由/页面。
  </how-to-verify>
  <resume-signal>Type "approved" or list gaps for Phase 2 readiness</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev server + browser | 本地登录态；截图不得进 git |
| auditor output → backlog | 严重度映射须一致 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03 | Information Disclosure | `.planning/ui-reviews/*.png` | mitigate | `.gitignore` gate Task 3；禁止 `git add` 截图 |
| T-01-04 | Repudiation | 审计结论 | accept | file:line 证据必填 |
| T-01-05 | Elevation | 误改 src | mitigate | 仅写 phase 目录 + ui-reviews gitignore |
| T-01-SC | Tampering | npm installs | accept | 无新依赖 |
</threat_model>

<verification>
- 三件套存在且中文正文 + 英文路径
- `01-UI-REVIEW.md` P0 Overall /24 与 D-13 一致
- 未执行全量 e2e smoke
- `npm test` 全绿
- 人工 checkpoint `approved`（D-15）
</verification>

<success_criteria>
- AUD-02 满足：六柱报告 + 分级可执行项
- D-09–D-19 落实；直接 spawn gsd-ui-auditor（非 stock ui-review）
- 团队认可足以支撑 Phase 2（checkpoint）
</success_criteria>

<output>
Create `.planning/phases/01-ux-audit-matrix/01-02-SUMMARY.md` when done
</output>
