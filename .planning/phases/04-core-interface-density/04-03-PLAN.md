---
phase: 04-core-interface-density
plan: 03
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - src/lib/workbench/cta-copy.ts
  - src/components/workbench/blueprint-editor.tsx
  - src/components/workbench/generate-drawer.tsx
  - src/app/(app)/sessions/[id]/workbench/workbench-client.tsx
autonomous: true
requirements:
  - WB-03
user_setup: []

must_haves:
  truths:
    - "确认蓝图 / 生成新版本 / 批量分析 文案全站一致（cta-copy SSOT）"
    - "蓝图未 confirmed 时生成阻断 toast + 引导至 compare 步"
    - "workbench + sessions 路由无装饰性 text-primary；无新增 text-[Npx]"
    - "npm test 全绿"
  artifacts:
    - path: "src/lib/workbench/cta-copy.ts"
      provides: "CTA label constants"
      exports: ["CONFIRM_BLUEPRINT_LABEL", "GENERATE_VARIANT_LABEL"]
    - path: "src/components/workbench/blueprint-editor.tsx"
      provides: "confirm uses SSOT + blueprintReadyToConfirm gate"
      pattern: "确认蓝图"
  key_links:
    - from: "blueprint-editor.tsx"
      to: "src/lib/workbench/cta-copy.ts"
      via: "import labels"
      pattern: "cta-copy"
---

## Phase Goal

**As a** 工作台用户, **I want to** 关键操作按钮层级与反馈在全站一致, **so that** 主 CTA 不被装饰色稀释。

<objective>
交付 WB-03：CTA SSOT、门禁 copy 统一、sessions/workbench 装饰 primary 与任意 px 扫尾（D-12–D-16）。

Wave 2；depends on 04-01 layout so confirm/generate sit in unified bars.
</objective>

<execution_context>
@D:/workflow-agent/.cursor/get-shit-done/workflows/execute-plan.md
@D:/workflow-agent/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-core-interface-density/04-CONTEXT.md
@.planning/phases/04-core-interface-density/04-RESEARCH.md
@src/components/workbench/blueprint-editor.tsx
@src/lib/blueprint/schema.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: cta-copy SSOT module</name>
  <files>src/lib/workbench/cta-copy.ts</files>
  <action>
    Create `cta-copy.ts` exporting:
    - `CONFIRM_BLUEPRINT_LABEL = "确认蓝图"`
    - `GENERATE_VARIANT_LABEL = "生成新版本"` (or locked project copy per RESEARCH)
    - `BATCH_ANALYZE_LABEL = "批量分析章节"` (or existing verb audit)
    Add minimal unit test if project pattern exists for lib/workbench
  </action>
  <verify>
    <automated>test -f src/lib/workbench/cta-copy.ts</automated>
    <automated>npm test</automated>
  </verify>
  <done>下游组件 import 常量而非硬编码字符串</done>
</task>

<task type="auto">
  <name>Task 2: BlueprintEditor + GenerateDrawer gates</name>
  <files>src/components/workbench/blueprint-editor.tsx, src/components/workbench/generate-drawer.tsx, workbench-client.tsx</files>
  <action>
    D-12/D-13:
    - Confirm button: `variant="default"` only when `blueprintReadyToConfirm`; else disabled + `type-caption` reason
    - Generate: use `GENERATE_VARIANT_LABEL`; blocked toast unified copy + navigate compare step
    - Batch analyze footer uses `BATCH_ANALYZE_LABEL`
  </action>
  <verify>
    <automated>rg -q 'cta-copy' src/components/workbench/blueprint-editor.tsx</automated>
    <automated>rg '确认蓝图' src/components/workbench --glob '*.tsx'</automated>
    <automated>npm test</automated>
  </verify>
  <done>主 CTA 门禁与文案一致</done>
</task>

<task type="auto">
  <name>Task 3: Token sweep — primary + arbitrary px</name>
  <files>src/app/(app)/sessions/**/*.tsx, src/components/workbench/**/*.tsx, src/components/sessions/**/*.tsx</files>
  <action>
    D-15/D-16:
    - `rg 'text-primary'` under sessions + workbench: 仅保留 Button default 主 CTA
    - `rg 'text-\['` 迁移到 type-* 或 remove
    - 不修改 shell/nav（Phase 3 范围）
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>主路径路由符合 02-UI-SPEC 色彩与排版禁令</done>
</task>

</tasks>
