---
phase: 04-core-interface-density
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - src/app/(app)/sessions/page.tsx
  - src/app/(app)/sessions/SessionsClient.tsx
  - src/components/sessions/project-card.tsx
  - src/components/projects/project-overview-page.tsx
autonomous: true
requirements:
  - WB-02
user_setup: []

must_haves:
  truths:
    - "/sessions 首屏 hero 合并或 space-y-10 分段，无并列双 surface-panel 竞争"
    - "指标区默认折叠或单行 caption 统计；首屏以项目列表为主"
    - "SessionsClient 单书区默认 Collapsible 收起；移除常驻「本页规则」侧栏 panel"
    - "项目卡片显示 deriveSessionDashboard 下一步 + 语义状态色"
    - "ProjectOverviewPage ≤2 panels；主 CTA 动词与工作台一致"
    - "npm test 全绿"
  artifacts:
    - path: "src/app/(app)/sessions/page.tsx"
      provides: "merged hero / collapsed metrics"
    - path: "src/app/(app)/sessions/SessionsClient.tsx"
      provides: "collapsible single-book section + no rules sidebar"
      pattern: "单书兼容"
  key_links:
    - from: "src/components/sessions/project-card.tsx"
      to: "src/lib/sessions/dashboard.ts"
      via: "nextHref + status badges"
      pattern: "deriveSessionDashboard"
---

## Phase Goal

**As a** 项目列表用户, **I want to** 快速扫读每个双书项目的状态与下一步, **so that** 列表页不像仪表盘堆叠。

<objective>
交付 WB-02：sessions 列表与项目概览留白重构（D-07–D-11）。

Depends on 04-01 workbench copy alignment but may execute in parallel after 04-01 merges.
</objective>

<execution_context>
@D:/workflow-agent/.cursor/get-shit-done/workflows/execute-plan.md
@D:/workflow-agent/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-core-interface-density/04-CONTEXT.md
@.planning/phases/04-core-interface-density/04-UI-SPEC.md
@.planning/phases/04-core-interface-density/04-PATTERNS.md
@src/app/(app)/sessions/page.tsx
@src/app/(app)/sessions/SessionsClient.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sessions page hero + metrics collapse</name>
  <files>src/app/(app)/sessions/page.tsx</files>
  <action>
    D-07/D-08:
    - 合并「主状态」与「快速入口」为单一 hero `surface-panel`（上下 `space-y-10` 子段）或删除其一
    - 4× MetricCard：收入 `Collapsible` 默认 closed，触发「项目统计」；或改为一行 `type-caption` 统计（3–4 项）
    - 移除 `FolderKanban` 装饰 `text-primary`（D-08）
    - `text-[28px]`/`text-[18px]`/`text-[13px]` → `type-display`/`type-title`/`type-body`
  </action>
  <verify>
    <automated>rg -c 'surface-panel' src/app/(app)/sessions/page.tsx | awk '{print ($1<=3)}'</automated>
    <automated>npm test</automated>
  </verify>
  <done>首屏 panel 层数减少；列表区更早进入视口</done>
</task>

<task type="auto">
  <name>Task 2: SessionsClient — collapsible single-book + hint</name>
  <files>src/app/(app)/sessions/SessionsClient.tsx</files>
  <action>
    D-09/D-10:
    - 单书分区包 `Collapsible` default closed；标题「单书兼容项目」
    - 删除/替换常驻「本页规则」侧栏 `surface-panel` → 列表顶单一 `HintBanner`（caption：下一步来自主路径）
    - 双书 `ProjectCard`：副标题用 dashboard `nextLabel`/`nextHref`；状态徽章用 `flash`/`info`/`locked`
    - 减少双栏权重：侧栏默认不渲染或仅 mobile
  </action>
  <verify>
    <automated>rg -q '单书兼容' src/app/(app)/sessions/SessionsClient.tsx</automated>
    <automated>! rg '本页规则' src/app/(app)/sessions/SessionsClient.tsx</automated>
    <automated>npm test</automated>
  </verify>
  <done>列表扫读成本降低；单书不抢双书注意力</done>
</task>

<task type="auto">
  <name>Task 3: Project overview density</name>
  <files>src/components/projects/project-overview-page.tsx</files>
  <action>
    D-11:
    - 收敛至 ≤2 `surface-panel`
    - 主 CTA：「进入工作台」→ workbench；「确认蓝图」条件与 workbench 一致
    - type-* 迁移；无新增任意 px
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>概览页与主路径动词一致</done>
</task>

</tasks>
