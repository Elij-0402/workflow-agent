---
phase: 04-core-interface-density
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(app)/sessions/[id]/workbench/workbench-client.tsx
  - src/components/workbench/pipeline-bar.tsx
  - src/app/(app)/sessions/[id]/workbench/loading.tsx
autonomous: true
requirements:
  - WB-01
user_setup: []

must_haves:
  truths:
    - "工作台 upload/analysis/compare/generate 四步首屏各仅一个主 CTA（default Button）"
    - "分析步 AnalysisCapabilityPanel 默认折叠，展开为单列摘要"
    - "对比步无 min-h-[680px]；BlueprintEditor 在 flex min-h-0 容器内"
    - "每步 section 使用 space-y-10；同一步 surface-panel ≤2（含合并 footer）"
    - "StepIntro 为动作导向单行 type-title（拆解章节 / 整理融合蓝图 / 生成变体）"
    - "workbench/loading.tsx 骨架对齐分析步主列布局"
  artifacts:
    - path: "src/app/(app)/sessions/[id]/workbench/workbench-client.tsx"
      provides: "四步密度收敛、Collapsible 能力面板、合并 footer"
      contains: "Collapsible"
    - path: "src/components/workbench/pipeline-bar.tsx"
      provides: "非当前步 muted glyph，无装饰性 text-primary"
    - path: "src/app/(app)/sessions/[id]/workbench/loading.tsx"
      provides: "批量导航 loading 骨架"
  key_links:
    - from: "src/app/(app)/sessions/[id]/workbench/workbench-client.tsx"
      to: "src/components/workbench/pipeline-bar.tsx"
      via: "compare/generate 步渲染"
      pattern: "PipelineBar"
    - from: "src/app/(app)/sessions/[id]/workbench/workbench-client.tsx"
      to: "src/components/workbench/blueprint-editor.tsx"
      via: "compare 步 flex 容器"
      pattern: "BlueprintEditor"
---

## Phase Goal

**As a** 双书项目用户, **I want to** 在工作台各阶段一眼看到当前该做的事, **so that** 非当前阶段说明默认收起、主路径不被六块面板淹没。

> ROADMAP `**Goal:**` 为叙述句；上句由 Phase 4 success criteria #1 与 `04-CONTEXT.md` D-01–D-06 推导。

<objective>
收敛工作台四步视觉分段与面板预算：Collapsible 能力摘要、步间留白、对比 flex 布局、生成空态 EmptySlot、上传语义色、route loading 骨架。

Purpose: 闭合审计 P0 #1–2、#36、#47、#55 与 WB-01；不改动 FlowStep、API、dual redirect（D-06）。
Output: 密度达标的工作台布局；`npm test` 全绿。
</objective>

<execution_context>
@D:/workflow-agent/.cursor/get-shit-done/workflows/execute-plan.md
@D:/workflow-agent/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-core-interface-density/04-CONTEXT.md
@.planning/phases/04-core-interface-density/04-RESEARCH.md
@.planning/phases/04-core-interface-density/04-PATTERNS.md
@.planning/phases/04-core-interface-density/04-UI-SPEC.md
@.planning/phases/02-minimal-design-contract/02-UI-SPEC.md
@.planning/phases/03-app-shell-navigation-ia/03-UI-SPEC.md
@src/components/app-nav.tsx
@src/app/(app)/sessions/[id]/workbench/workbench-client.tsx
@src/components/workbench/pipeline-bar.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Step rhythm, StepIntro, PipelineBar</name>
  <files>src/app/(app)/sessions/[id]/workbench/workbench-client.tsx, src/components/workbench/pipeline-bar.tsx</files>
  <read_first>src/components/app-nav.tsx, src/components/workbench/pipeline-bar.tsx, .planning/phases/04-core-interface-density/04-UI-SPEC.md</read_first>
  <action>
    **workbench-client.tsx**（D-01、D-05、D-06、D-17）：
    - 四个 `activeStep` 的 `section` 根 class：`space-y-5` → `space-y-10`。
    - `StepIntro`：`h2` 改用 `type-title`；`description` 用 `type-body`；替换标题 copy（单行动作导向，禁止「第 N 步 ·」前缀）：
      - upload: **上传双书素材**（description 保持双书到位语义）
      - analysis: **拆解章节**
      - compare: **整理融合蓝图**
      - generate: **生成变体**
    - **保留** PageHeader「项目概览」链与 `?view=overview` href；仅 `type-*` 抛光间距（D-06）。
    - **不要** 改 `navigateToStep`、`WorkflowStageBar` 键位、confirm/generate API。

    **pipeline-bar.tsx**（D-18、D-15 预清理）：
    - 非完成、非当前步 glyph：`text-primary` → `text-muted-foreground/55`；分隔符 `text-primary/30` → `text-muted-foreground/40`。
    - `text-[12px]` → `type-caption`。
    - 当前步：`text-foreground`（非装饰性 primary）。

    本 task **不** 改 `AnalysisCapabilityPanel` 结构（Task 2）。
  </action>
  <verify>
    <automated>rg -q 'space-y-10' "src/app/(app)/sessions/[id]/workbench/workbench-client.tsx"</automated>
    <automated>rg -q '拆解章节' "src/app/(app)/sessions/[id]/workbench/workbench-client.tsx"</automated>
    <automated>rg -v '^#' "src/components/workbench/pipeline-bar.tsx" | rg 'text-primary' ; if ($LASTEXITCODE -eq 0) { exit 1 }</automated>
    <automated>npm test -- src/lib/workbench/derive-hint.test.ts</automated>
  </verify>
  <done>四步 StepIntro 为单行动作标题；步间 space-y-10；PipelineBar 无装饰性 primary。</done>
</task>

<task type="auto">
  <name>Task 2: Analysis step — Collapsible capability + footer budget</name>
  <files>src/app/(app)/sessions/[id]/workbench/workbench-client.tsx</files>
  <read_first>src/components/app-nav.tsx, .planning/phases/04-core-interface-density/04-PATTERNS.md</read_first>
  <action>
    **AnalysisCapabilityPanel**（D-02）：
    - 删除 props `expanded`/`onToggle` 与父级 `analysisGuideExpanded` state。
    - 用 `@/components/ui/collapsible`（对齐 `app-nav.tsx`）：`useState(false)`，**默认关闭**。
    - `CollapsibleTrigger`：eyebrow + 单行 `type-title` 摘要（`guide.shortSummary`）+ `ChevronRight` 旋转。
    - `CollapsibleContent`：**单列** `space-y-4` 堆叠 `InfoListCard`；**删除** `lg:grid-cols-3` 与 12px 密集格。
    - 折叠态首屏不渲染三列卡；`StageLine`/`InfoListCard` 装饰 `text-primary/80` → `text-muted-foreground` 或 `type-caption`。

    **分析步布局**（D-01、D-17）：
    - 保留 `HintBanner`、`BatchTracker`、`analysisGrid`；批量主按钮留在 `analysisGrid`（chapter-tree），勿在 footer 再叠第二个 default 主按钮。
    - 底部仅 **一个** `surface-panel` footer：「前往对比」+ status `type-body`；计为分析步第 2 层 panel（能力面板折叠时不占首屏视觉权重）。
    - 能力面板外层 **不要** 再包额外 `surface-panel`（展开内容在 CollapsibleContent 内，触发器可用 `surface-subtle` 或无边框条）。

    **不要** 改 batch fetch、abort、章节 API。
  </action>
  <verify>
    <automated>rg -q 'Collapsible' "src/app/(app)/sessions/[id]/workbench/workbench-client.tsx"</automated>
    <automated>rg -v '^#' "src/app/(app)/sessions/[id]/workbench/workbench-client.tsx" | rg 'lg:grid-cols-3' ; if ($LASTEXITCODE -eq 0) { exit 1 }</automated>
    <automated>rg 'analysisGuideExpanded' "src/app/(app)/sessions/[id]/workbench/workbench-client.tsx" ; if ($LASTEXITCODE -eq 0) { exit 1 }</automated>
    <automated>npm test</automated>
  </verify>
  <done>能力面板默认折叠、展开单列；分析步 ≤2 层 surface-panel；批量主 CTA 仍在 analysisGrid。</done>
</task>

<task type="auto">
  <name>Task 3: Compare/upload/generate layout + loading skeleton</name>
  <files>src/app/(app)/sessions/[id]/workbench/workbench-client.tsx, src/app/(app)/sessions/[id]/workbench/loading.tsx</files>
  <read_first>src/components/workbench/blueprint-editor.tsx, .planning/phases/04-core-interface-density/04-UI-SPEC.md</read_first>
  <action>
    **对比步**（D-03、D-07 backlog #51）：
    - 删除 `min-h-[680px]` 包裹；改为 `flex min-h-0 flex-1 flex-col`（父级若需 `flex flex-col` 链则补齐）。
    - compare footer `surface-panel` **仅**保留「前往生成」+ muted 说明；**不要** 在 footer 放「确认蓝图」（确认仅在 `BlueprintEditor` 顶栏，留给 04-03 统一 disabled 规则）。
    - `CollapsedChaptersBar`「回看分析 →」：`text-primary` → `text-muted-foreground` 或 `group-hover:text-foreground`。

    **上传步**（D-04、backlog #47）：
    - `UploadBookCard` / health 条：`amber-*`/`sky-*`/`text-amber-300`/`text-sky-300` → `text-warning`、`text-info`、`border-warning/30`、`bg-warning/5` 等语义 token（02-UI-SPEC）。
    - upload footer 保留单一 primary（`uploadSummary.actionLabel`）；文案 class → `type-body`。

    **生成步**（D-04、#55）：
    - 无变体时用 `EmptySlot` 模式（与 upload B 位一致）：步骤编号 + `type-title`/`type-body` + 主按钮占位（按钮 copy 统一留给 04-03，本 task 可先接 `生成新版本` 或保留 disabled 态结构）。
    - 删除生成步多余 `surface-panel` 说明层：变体列表区 + **至多** 一个 action 区；空态不叠第三层 panel。
    - `text-[20px]`/`text-[13px]`/`text-[12px]` → `type-title`/`type-body`/`type-caption`（本文件 touched 区域）。

    **loading.tsx**（D-14 骨架部分）：
    - 增强 `PageLoadingShell` 或本地 skeleton：模拟 **分析步** 主列（1 宽内容 + 底栏 skeleton），供 batch 导航等待；勿改 RSC data path。

    **禁止** 新增 `min-h-[Npx]`、`text-[Npx]`（D-07 例外 loading 骨架已有则保留 PageLoadingShell 约定）。
  </action>
  <verify>
    <automated>rg 'min-h-\[680' "src/app/(app)/sessions/[id]/workbench/workbench-client.tsx" ; if ($LASTEXITCODE -eq 0) { exit 1 }</automated>
    <automated>rg -v '^#' "src/app/(app)/sessions/[id]/workbench/workbench-client.tsx" | rg -E 'amber-|sky-' ; if ($LASTEXITCODE -eq 0) { exit 1 }</automated>
    <automated>rg -q 'workbench/loading' "src/app/(app)/sessions/[id]/workbench/loading.tsx"</automated>
    <automated>npm test</automated>
  </verify>
  <done>对比 flex 无 680px；上传语义色；生成空态 EmptySlot；loading 骨架存在；工作台 touched 区无 amber/sky。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → workbench client | 布局/折叠仅影响展示；API 与 redirect 不变 |
| RSC → workbench page | 无新服务端输入 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | workbench batch handlers | accept | 不修改 fetch URL/body |
| T-04-02 | Information disclosure | Collapsible 摘要 | accept | 仅已有 guide 文案 |
| T-04-03 | Denial of service | — | accept | 无新网络面 |
| T-04-SC | Tampering | npm installs | mitigate | 本 plan 无新包；Collapsible 已 Phase 3 安装 |

</threat_model>

<verification>
- `npm test` 全绿
- `rg 'min-h-\[680' src/app/(app)/sessions/[id]/workbench/workbench-client.tsx` 无匹配
- 手动：分析步首屏见 Pipeline/树/批量区，能力面板默认折叠；对比步 Blueprint 可滚动
</verification>

<success_criteria>
- WB-01：D-01–D-06、D-17–D-18 布局与分段满足 `04-UI-SPEC.md` § WB-01
- 不回归 Phase 3 dual redirect 与壳层 CTA
</success_criteria>

<output>
Create `.planning/phases/04-core-interface-density/04-01-SUMMARY.md` when done
</output>
