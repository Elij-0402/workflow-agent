---
phase: 04-core-interface-density
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(app)/sessions/[id]/workbench/workbench-client.tsx
  - src/app/(app)/sessions/[id]/workbench/loading.tsx
  - src/components/workbench/pipeline-bar.tsx
autonomous: true
requirements:
  - WB-01
user_setup: []

must_haves:
  truths:
    - "分析步首屏仅 PipelineBar + 章节/批量焦点 + 单一底部主 CTA；AnalysisCapabilityPanel 默认折叠"
    - "对比步无 min-h-[680px]；页脚合并为一条 StepActionBar（非重复 surface-panel 说明）"
    - "StepIntro 标题为动作导向单行（拆解章节 / 整理融合蓝图 / 生成变体）"
    - "workbench/loading.tsx 为工作台形态 skeleton（非泛型 PageLoadingShell  alone）"
    - "npm test 全绿"
  artifacts:
    - path: "src/app/(app)/sessions/[id]/workbench/workbench-client.tsx"
      provides: "per-step density: ≤2 surface-panel per step, space-y-10 between sections"
      pattern: "Collapsible"
    - path: "src/app/(app)/sessions/[id]/workbench/loading.tsx"
      provides: "workbench-shaped route loading"
      contains: "WorkflowStageBar|skeleton"
  key_links:
    - from: "workbench-client.tsx"
      to: "src/components/ui/collapsible.tsx"
      via: "AnalysisCapabilityPanel default closed"
      pattern: "Collapsible"
    - from: "workbench-client.tsx"
      to: "src/lib/workbench/derive-hint.ts"
      via: "HintBanner text"
      pattern: "deriveHint"
---

## Phase Goal

**As a** 双书项目用户, **I want to** 在工作台各阶段一眼看到当前任务与唯一主操作, **so that** 分析/蓝图/生成不被同屏面板淹没。

<objective>
交付 WB-01 工作台密度切片：折叠能力说明、每步单焦点 action bar、移除 compare 步硬编码 min-height、升级 loading skeleton、StepIntro 与 type-* 迁移（本 plan 内 workbench 路由文件）。

Purpose: 闭合审计 P0 #1–2、P1 #5、#7、#9（workbench 范围）。
Output: 可扫读的三步分段工作台，行为与 API 不变。
</objective>

<execution_context>
@D:/workflow-agent/.cursor/get-shit-done/workflows/execute-plan.md
@D:/workflow-agent/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-core-interface-density/04-CONTEXT.md
@.planning/phases/04-core-interface-density/04-RESEARCH.md
@.planning/phases/04-core-interface-density/04-PATTERNS.md
@.planning/phases/04-core-interface-density/04-UI-SPEC.md
@.planning/phases/02-minimal-design-contract/02-UI-SPEC.md
@src/app/(app)/sessions/[id]/workbench/workbench-client.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Analysis step — Collapsible capability + single action bar</name>
  <files>src/app/(app)/sessions/[id]/workbench/workbench-client.tsx</files>
  <read_first>04-UI-SPEC.md § Analysis step, app-nav.tsx Collapsible pattern, AnalysisCapabilityPanel in workbench-client.tsx</read_first>
  <action>
    Refactor `AnalysisCapabilityPanel` (D-02):
    - Wrap in shadcn `Collapsible` with `defaultOpen={false}`; trigger「分析能力说明」`type-mono-label` + Chevron
    - Expanded content: **单列**摘要（禁止 3 列 12px 格）；迁移 `text-[Npx]` → `type-caption`/`type-body`
    - 分析步主体：`PipelineBar` + `ChapterTree`/batch 区 + **一个**底部 `StepActionBar`（sticky footer，**不**再包一层 `surface-panel` 说明块，D-17）
    - 批量分析主 CTA 仅在 footer；进行中保留 `BatchTracker`
    - `StepIntro` 标题改为「拆解章节」（单行，D-05）
    - 步骤容器 `space-y-10`（D-17）
  </action>
  <verify>
    <automated>rg -q 'Collapsible' src/app/(app)/sessions/[id]/workbench/workbench-client.tsx</automated>
    <automated>! rg 'min-h-\[680px\]' src/app/(app)/sessions/[id]/workbench/workbench-client.tsx || echo compare step handled in task 2</automated>
    <automated>npm test</automated>
  </verify>
  <done>分析步默认可折叠能力面板；同屏 surface-panel ≤2；单一主 CTA 在 footer</done>
</task>

<task type="auto">
  <name>Task 2: Compare + generate steps — layout + StepIntro + tokens</name>
  <files>src/app/(app)/sessions/[id]/workbench/workbench-client.tsx, src/components/workbench/pipeline-bar.tsx</files>
  <action>
    Compare step (D-03):
    - 移除 `min-h-[680px]`；外层 `flex flex-col min-h-0 flex-1`
    - 合并底部 duplicate `surface-panel` 为一条 `StepActionBar`（与 BlueprintEditor 内 confirm 不重复文案）
    - `StepIntro` →「整理融合蓝图」
    - `CollapsedChaptersBar` 保留

    Generate step (D-04):
    - `StepIntro` →「生成变体」
    - 空态对齐 `EmptySlot` 步骤编号
    - 上传步状态条：amber/sky → `text-info`/`text-warning` 或语义 badge（D-04, backlog #47）

    PipelineBar (D-18):
    - 分隔符 `text-primary/30` → `text-muted-foreground/30`（非装饰 primary）

    Workbench-wide: 触及的 `text-[Npx]` 迁移到 `type-*`；装饰性 `text-primary` 从非 Button 元素移除（D-15 局部，04-03 可扫尾）
  </action>
  <verify>
    <automated>! rg 'min-h-\[680px\]' src/app/(app)/sessions/[id]/workbench/workbench-client.tsx</automated>
    <automated>rg '整理融合蓝图|生成变体|拆解章节' src/app/(app)/sessions/[id]/workbench/workbench-client.tsx</automated>
    <automated>npm test</automated>
  </verify>
  <done>compare/generate 步留白与标题符合 UI-SPEC；无 680px 硬编码</done>
</task>

<task type="auto">
  <name>Task 3: Workbench loading skeleton</name>
  <files>src/app/(app)/sessions/[id]/workbench/loading.tsx</files>
  <action>
    Upgrade existing `loading.tsx` (RESEARCH: file exists):
    - 渲染 `PageLoadingShell` 或自定义 skeleton：顶栏占位 + `WorkflowStageBar` 条 + 双列内容区 pulse
    - 匹配 `space-y-10` 节奏；无业务逻辑
  </action>
  <verify>
    <automated>test -f src/app/(app)/sessions/[id]/workbench/loading.tsx</automated>
    <automated>rg -q 'skeleton|animate-pulse' src/app/(app)/sessions/[id]/workbench/loading.tsx</automated>
  </verify>
  <done>长批量/导航时工作台有结构反馈</done>
</task>

</tasks>
