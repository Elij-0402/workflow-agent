# samples/

Local-only test fixtures (sample novels, etc.).

This directory is gitignored except for this README. Drop any `.txt` novel
files you want to use during local upload/analysis testing here — they will
not be committed.

Originally populated with `1508.txt` and `《斗破苍穹》小说全集txt版.txt` that
were previously tracked under `docs/`.
